# 修复记录 - 2026-04-23

## 已完成的修复

### P0 - 安全问题

#### ✅ 删除 debugUsers 云函数
- **问题**: 可枚举用户邮箱和内部 ID，存在数据泄露风险
- **修复**: 删除 `cloudfunctions/debugUsers/` 目录
- **影响**: 消除数据泄露风险

---

### P1 - 数据一致性问题

#### ✅ 统一 assessments 顺序约定
- **问题**: 前后端对 assessments 数组顺序理解不一致，导致趋势对比可能错误
- **根因**: 
  - 后端按 `createdAt asc` 返回（最旧在前，最新在后）
  - 前端部分代码假设"数组末尾是最新"，部分代码做了反转
- **修复**:
  1. 在云函数中添加注释明确约定：`orderBy('createdAt', 'asc')` 返回正序数组
  2. 在前端所有使用 assessments 的地方添加注释说明顺序约定
  3. 确保趋势对比统一使用 `assessments[length-2]` 和 `assessments[length-1]`
- **修改文件**:
  - `cloudfunctions/getCases/index.js`
  - `cloudfunctions/getCaseDetail/index.js`
  - `src/pages/case-detail/case-detail.vue`
  - `src/pages/timeline/timeline.vue`
  - `src/pages/assessments/assessments.vue`
- **约定**: 
  - **后端**: 始终按 `createdAt asc` 返回，数组末尾是最新评估
  - **前端**: 需要倒序展示时（如评估历史页），显式调用 `reverse()`

#### ✅ 恢复 createTimeline 事务
- **问题**: 事务逻辑被注释掉，写入失败时无法回滚，可能导致数据不一致
- **根因**: AI 调用耗时过长导致事务超时
- **修复策略**: 
  1. 将 AI 调用（`recalculateAssessmentFromEvent`）移到事务外执行
  2. 将趋势记录预计算（`buildTrendTimelineRecords`）移到事务外执行
  3. 仅在事务内执行数据库写入操作：
     - 写入 timeline_records（用户事件）
     - 写入 assessments（评估结果）
     - 写入 timeline_records（系统趋势记录）
     - 更新 cases.latestResultId
  4. 恢复事务的 commit 和 rollback 逻辑
- **修改文件**: `cloudfunctions/createTimeline/index.js`
- **优势**:
  - 事务仅包含快速的数据库操作，避免超时
  - 失败时自动回滚，保证数据一致性
  - AI 调用失败不影响事务，可单独处理

---

## 修复验证建议

### 功能测试
1. **创建时间线测试**:
   - 创建案例 → 添加时间线事件 → 验证评估自动更新
   - 验证系统趋势记录正确生成
   - 模拟 AI 调用失败场景，验证事务回滚

2. **趋势对比测试**:
   - 创建案例 → 多次重评 → 验证详情页趋势对比正确
   - 验证评估历史页显示顺序正确（最新在前）
   - 验证时间线页即时反馈正确

3. **安全测试**:
   - 验证 debugUsers 云函数已无法访问
   - 尝试并发创建时间线，验证事务隔离性

### 压力测试
- 并发创建时间线 → 验证事务不冲突
- AI 调用超时场景 → 验证错误处理和回滚

---

## 技术细节

### 事务优化前后对比

**优化前**:
```javascript
// AI 调用在事务内，导致超时
transaction = await db.startTransaction()
const recalculated = await recalculateAssessmentFromEvent(...) // 可能耗时 5-10 秒
await transaction.collection('assessments').add(...)
await transaction.commit() // 超时失败
```

**优化后**:
```javascript
// AI 调用在事务外
const recalculated = await recalculateAssessmentFromEvent(...) // 耗时操作
const autoRecords = buildTrendTimelineRecords(...) // 预计算

// 事务仅包含快速的数据库写入
transaction = await db.startTransaction()
await transaction.collection('timeline_records').add(...)
await transaction.collection('assessments').add(...)
for (const record of autoRecords) {
  await transaction.collection('timeline_records').add(...)
}
await transaction.collection('cases').doc(caseId).update(...)
await transaction.commit() // 快速完成
```

### assessments 顺序约定

**后端返回**:
```javascript
// getCaseDetail/index.js
const { data: assessments } = await db.collection('assessments')
  .where({ caseId })
  .orderBy('createdAt', 'asc') // 正序：[旧 → 新]
  .get()
// assessments = [评估1, 评估2, 评估3] (评估3 最新)
```

**前端使用**:
```javascript
// case-detail.vue - 趋势对比
const previous = assessments[assessments.length - 2] // 评估2
const current = assessments[assessments.length - 1]  // 评估3

// assessments.vue - 倒序展示
const reversed = [...assessments].reverse() // [评估3, 评估2, 评估1]
```

---

## 剩余问题（非阻塞）

### P2 - 体验优化
1. **时间线 hash 定位**: 点击焦点证据后无法滚动到目标事件
2. **删除成功提示**: 需要在 cases 页面读取 `casesDeleted*密码规则**: 前后端统一为至少 8 位
4. **重评页字段**: 禁用名称/画像编辑或实现提交逻辑

这些问题不影响核心功能，可在后续迭代中修复。

---

## 上线检查清单

- [x] P0 安全问题已修复（debugUsers 已删除）
- [x] P1 数据一致性问题已修复（事务恢复 + 顺序统一）
- [ ] 功能测试通过
- [ ] 压力测试通过
- [ ] 部署到测试环境验证

完成测试后即可进入灰度上线阶段。
