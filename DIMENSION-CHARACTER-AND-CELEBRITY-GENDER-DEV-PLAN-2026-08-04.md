# 名人与次元角色图鉴开发计划

> 执行状态（2026-08-05）：**代码、规则测试、相关云函数和题库索引已实施；名人 `1.1.0` 与次元角色 `1.0.1` 已发布。** 本文记录性别版本的最终约束，后续修改必须同步更新题库版本、校准报告和交接文档。

日期：2026-08-04
分支：`codex/archetype-tests-development`
目标环境：`cloud1-d0gvhqu2c8a2b61fd`

## 1. 交付范围

### 1.1 Crush 名人图鉴升级

- 登录后才可使用。
- 支持“测自己”和“测当前 Crush”。
- 测自己时，服务端读取 `users.selfProfile.gender`。
- 测 Crush 时，服务端读取当前用户拥有的 `cases.profile.gender`。
- 性别只允许 `male` / `female`；缺失或未知时返回 `PROFILE_GENDER_REQUIRED`，前端引导补全对应画像。
- 不接受客户端指定或覆盖性别，不在测试页提供临时性别选择器。
- 只在与被测对象同性别的人物中计算排名、前五名和相似度。
- 名人题库升级为 `1.1.0`，48 人保持 24 男 + 24 女，并重新完成分性别校准。

### 1.2 次元角色图鉴

- 新功能键：`次元角色图鉴`。
- 新结果类型：`dimension_character`。
- 新算法版本：`dimension-character-v1`。
- 登录、测试对象、性别来源、当前 Crush 锁定规则与名人图鉴一致。
- 独立题库、版本、发布状态、校准报告、结果记录、后台管理和页面路由。
- 首发 72 个角色，严格 36 男 + 36 女。
- 覆盖经典名著、金庸武侠、盗墓题材、国产影视、国际书影音、动漫六类；兼顾年轻、中年与全年龄用户。
- 包含英雄、灰度和反派角色，名单必须覆盖西门庆、岳不群、祁同伟，以及《龙珠》、金庸、《盗墓笔记》《北派盗墓笔记》等明确要求的作品。
- 结果显示相似度百分比、五维画像、前五名、吸引点和相处提醒。

## 2. 统一测试模型

- 五维：主动度、温暖度、可靠度、浪漫度、边界感。
- 每套 12 题，每题 A-D；测 Crush 时允许“不确定”。
- 自测文案和测 Crush 文案分别配置，题目使用生活化场景，避免理论术语。
- 得分由服务端根据已发布题库计算；客户端只提交题号和选项。
- 人物匹配使用加权欧氏距离并转换为 `0..100%` 相似度。
- 同性别候选池不足或题库性别配置错误时拒绝计算。

## 3. 数据与接口

### 3.1 题库人物字段

通用字段：

- `key`：发布后稳定，不允许后台改写。
- `name`：后台可修改的显示名称。
- `gender`：`male` / `female`。
- `profile`：五维 `0..100` 向量。
- `enabled`、`sortOrder`、`coverUrl`。

角色扩展字段：

- `source`：作品名。
- `category`：`classic | wuxia | tomb_raiding | chinese_screen | international | anime`。
- `audienceTags`：`youth | middle_age | all_age` 数组。
- `heatLevel`：`1..5`。
- `alignment`：`heroic | gray | villain`。
- `summary`、`attraction`、`caution`。

### 3.2 结果字段

- 两种图鉴结果均保存 `subjectGender`。
- `dimension_character` 保存独立 `algorithmVersion`。
- 保存人物 key、相似度表、前五、五维、作答数量、置信度、题库版本、被测 Crush 快照。

### 3.3 错误码

- `AUTH_REQUIRED`：未登录。
- `FEATURE_NOT_AVAILABLE`：套餐未开放。
- `CASE_NOT_FOUND`：当前 Crush 缺失或不属于当前用户。
- `PROFILE_GENDER_REQUIRED`：被测对象画像性别缺失/未知。
- `CONTENT_VERSION_MISMATCH`：题库未发布、版本错误或校验失败。

## 4. 权限与配置

- `次元角色图鉴` 加入后台订阅配置的 `features/excludedFeatures`。
- 仅在配置缺少新键时补默认值，绝不覆盖管理员现有配置。
- 默认：试用开放、免费关闭、Pro 开放、Ultra 开放。
- 配置版本升级到 `7`。

## 5. 前端与入口

- 不新增首页 Banner。
- 在“桃花人设”区域增加“次元角色图鉴”入口，并保留关系主角、名人图鉴入口。
- 在 Crush 详情页增加测当前 Crush 的次元角色入口。
- 测试页不允许切换 Crush；需要切换时返回“我们”或 Crushes 页面操作。
- 自己性别缺失：按钮跳转 `/pages/edit-profile/edit-profile`。
- Crush 性别缺失：按钮跳转当前 Crush 的资料编辑流程。
- 新增测试、结果、人物详情、历史页面，并注册 `pages.json`。
- 页面沿用现有 Campus Pop 主题、44px 触控目标、清晰焦点与移动端无横向滚动规则。

## 6. 后台管理

- 题库管理新增“次元角色图鉴”功能页签。
- 名人和角色人物均可编辑显示名称、封面、启用状态、性别和五维。
- 角色可编辑作品、分类、受众、热度、阵营、摘要、吸引点、提醒。
- 稳定 key 不允许在表单中修改。
- 两种图鉴草稿均支持结构校验、20 万组分性别校准和发布。

## 7. 校准要求

- 名人和角色分别对男、女候选池运行至少 20 万组模拟。
- 每个人必须有可稳定成为第一名的 golden answers。
- 允许单项向量在原值基础上不超过 `±5` 调整。
- 保存调整前后报告，包含每项差值、分性别赢家分布、最大占比、前五占比、缺失 golden answers、报告校验和。
- 发布时校验报告与当前题库 checksum 一致。

## 8. 测试与验收

- 题库结构：名人 48/24+24；角色 72/36+36；key、排序、元数据完整唯一。
- 评分：结果只含被测对象同性别人物；客户端伪造性别无效。
- 画像：自己或 Crush 性别未知均返回 `PROFILE_GENDER_REQUIRED`。
- 权限：试用/免费/Pro/Ultra 默认规则正确，已有后台配置不被覆盖。
- 管理：导入、建草稿、保存、校验、校准、发布、归档事务通过。
- 结果：两类图鉴可保存、查询单条与历史，越权查询失败。
- 构建：`npm.cmd run test:archetypes`、校准测试、H5、微信小程序全部通过。
- 部署：同步 `_shared` 后部署正式函数，发布新题库，审计题库状态、索引和结果集合。

## 9. 执行顺序

1. 测试契约与共享评分模型。
2. 名人性别数据、服务端画像性别校验和 `1.1.0` 校准。
3. 72 人角色题库、独立校准和后台题库能力。
4. 前端页面、入口、历史、权限配置。
5. 全量测试、构建、CloudBase 部署、发布与审计。
