# 2026-06-08 工作总结

## 一、命理桃花独立模块开发（Phase 1）

- 创建 `src/utils/taohua.ts`（645 行）：前端查表算法层，端口 Python 引擎的咸池/红鸾/星座双维度/属相×星座交叉全部静态查表
- 创建 `src/components/TaohuaCompass.vue`（135 行）：CSS 纯绘制传统罗盘，8 方位臂 + 12 地支标签 + 24 山刻度 + 八卦 + 天池指针，深色铜质风格
- 创建 `src/pages/taohua/taohua.vue`（1052 行）：7 张卡片的完整命理页面，包含罗盘、行动指南、宜忌、桃花人设、属相×星座匹配、桃花方位全览
- 注册路由 `pages/taohua/taohua`，首页加临时跳转入口
- Mock 数据来自 Python 引擎真实输出（兔+双鱼座）

## 二、云函数数据层（Phase 2）

- 创建 `cloudfunctions/queryTaohua/`：基于 lunar-javascript（寿星天文历 JS 移植），返回日柱/方位/宜忌/节气/星宿等日历数据
- 验证 JS 输出与 Python lunar-python 完全一致（日柱/喜神/建除/宜忌/星宿逐字段对齐）
- 部署成功，前端混合模式：云函数优先 → 降级 mock

## 三、增强功能（Phase A/B/C/D）

### A. 桃花指数（0-100 评分）
- 算法：基础 50 分 + 建除加权（成/开+30，破/闭-30）+ 桃花喜神重合+20 + 天喜日+15 + 宜忌感情项加减
- 评级：🔥 爆棚(≥85) / 🌤️ 不错(≥70) / 😐 平常(≥55) / 🌧️ 偏低(≥40) / ⚠️ 低迷(≥25) / ❄️ 冰点(<25)
- 前端展示：大号分数 + 进度条 + 评级标签

### B1. 双人解读增强
- `generatePairInsight()` 生成风格碰撞 + 适合一起做的事 + 注意事项 + 古籍依据
- 配对 bottom sheet 从干巴巴的"三合"升级为 4 段深度解读

### B2. Canvas 分享卡
- 500×400 brutalist 风格图片：生肖 emoji + 星座 + 星次信息 + 桃花风格 + 品牌底部
- 保存到相册 / 转发好友

### C. 双层穿戴加成
- 日支五行 × 本命五行（用户生肖）联动分析
- 25 种五行关系映射（生/克/同），每种给出具体穿搭配色建议
- 例：兔(木) × 今日桃花丑(金) → 金克木 → "穿白色+绿色，金银和木质叠戴化解克制"

### D. 每日推送
- 创建 `cloudfunctions/sendTaohuaNotify/`：定时触发器（每天 7:55），向已订阅用户推送今日桃花方位+宜忌+穿搭
- 创建 `taohua_subscriptions` 集合，支持订阅/取消/自动失效
- 前端加订阅按钮（需微信模板审批后启用）

## 四、约会建议自适应

- 约会方位根据桃花指数自动调整推荐强度：
  - ≥70 分：大胆推荐具体场所
  - 40–69 分：温和推荐
  - <40 分：线上为主，不建议线下约会
- 解决了"指数 33 分还推荐具体约会场所"的矛盾

## 五、古籍出处标注

- 方案 A（卡片级）：罗盘卡片底部加黄色左边框出处引用
- 方案 B（行级）：行动指南每条标题旁加灰色小标签标注古籍来源
- 创建 `pages/references/references.vue`（引用经典页面）：列出 4 本中国经典 + 1 本西方经典，标注作者/年代/具体引用内容
- 入口：我 → 引用经典（在系统反馈和关于之间）

## 六、Bug 修复

- 罗盘手臂方向修正（从向下改为向上，0°=北）
- 罗盘标签从手臂上移到外圈固定位置（始终可读）
- 方位字段兼容（云函数 `fangwei` vs 模板 `方位`）
- 手臂高亮匹配（"正南"→"南"映射）
- 约会低分建议未更新（mock 缓存问题）

## 七、Python 引擎增强

- `mingli_engine.py` 新增函数：`_calc_taohua_score()`、`_build_practical_guide()`、`_get_day_vibe()`
- 12 种建除对应 12 套详细的活动建议（宜做/避开/建议活动/一句话）
- 行动指南输出结构：约会方位 + 活动建议 + 双层穿戴建议
- 所有改动同步到 JS 云函数

## 八、桃花罗盘视觉重设计

- 参考传统智能罗盘截图，将 `TaohuaCompass.vue` 从深色简版罗盘改为金色多层传统罗盘
- 增加二十四山、八方、十二地支、八卦圈、刻度环、天池中心等视觉层级
- 保持动态能力：`direction` 控制桃花位箭头方向，`directionZhi` 控制对应地支高亮
- 修正语义问题：中心天池不再显示“午”等地支，避免误导用户以为地支在中央
- 删除中心斜线磁针装饰，避免与真实桃花方位指针冲突
- 十字天心线拆为四段，延伸到中心红圈边缘，不穿过天池
- 指针改为内圈指南针式单箭头，仅在中心内圈显示，箭头指向今日桃花位
- 调整八卦圈与中心圆盘距离，避免“乾、坎、艮”等文字被中心圆遮挡

## 九、桃花人格分享图重设计

- 将“分享我的桃花人格卡”从 500×400 横版信息卡升级为 640×960 竖版海报
- 新版海报结构：顶部品牌区、主标题、生肖/星座/星次标签、吸引力关键词、桃花风格、西方星座、中国星次、高频适配、底部 CTA
- 使用 canvas 绘制圆角纸张、柔和背景、罗盘水印、印章头像、胶囊标签和分区卡片
- 增加换行限制函数，避免长文案溢出
- 根据反馈修复遮挡：
  - “桃花风格 / 西方星座 / 中国星次”标题不再用黑色胶囊压在正文上
  - 改为模块标题 + 分割线 + 正文内容
  - 正文整体下移，模块高度加大
  - 底部“AI 辅助分析”文案下移，避开框线

## 十、分享落地页与未登录链路

- 新增公开分享页 `src/pages/taohua-share/taohua-share.vue`
- 注册路由 `pages/taohua-share/taohua-share`
- 分享出去后，好友未登录也能先看到 TA 的桃花人格卡，而不是直接被登录页拦截
- 分享页通过 URL 参数 `zodiac/sign` 还原生肖、星座、桃花人格、星次和适配标签
- “我也测一下”按钮根据登录状态分流：
  - 未登录：进入登录页
  - 已登录：进入本人画像页
- `login.vue` 增加 `redirect` 参数支持，登录后可回跳到目标页
- `self-profile.vue` 增加 `redirect` 参数支持，新用户完成画像后可回到桃花页
- `taohua.vue` 增加 `onShareAppMessage`，分享路径指向公开落地页，并优先使用生成海报作为小程序卡片缩略图

## 十一、构建验证

- `npm.cmd run build:h5` 通过
- `npm.cmd run build:mp-weixin` 多次通过
- 微信小程序构建输出目录：`dist/build/mp-weixin`

## 十二、命理桃花 UI 审计结论（未改代码）

- 发现 `pages/taohua/taohua` 已使用系统导航栏，但页面内部又自绘 `nav-bar-v2`，导致微信小程序中出现系统返回 + 页面内返回的重复返回入口
- 建议删除 `taohua.vue` 顶部自定义导航栏及对应 `goBack()` / `.nav-bar-v2` 样式，保留系统导航标题“命理桃花”
- 命理桃花主页面整体基本符合现有 v2 / Campus Pop 设计系统：
  - 黑边框、硬阴影、黄色强调色、`card-v2`、`section-title-v2`、`btn-v2-me` 等结构和全站一致
  - 字体层级大体符合现有规范：section title 22rpx、正文 22/24rpx、按钮 26rpx
- 分享落地页 `taohua-share.vue` 有一定视觉偏离：
  - 使用了独立的 `.hero/.poster/.tag/.cta-card` 体系，没有复用 `v2-mode/card-v2/tag-v2/btn-v2` 命名和节奏
  - 整体仍是黑边框硬阴影，但更像独立传播海报页，而不是标准 app 内页
- 颜色审计：
  - 主功能页 `#FF6B6B + #FFD93D + #111` 基本贴合 campus-pop
  - 分享页额外使用较多棕红/暖粉色（如 `#7f2b1d`、`#8a3a28`、`#ffe2d8`、`#fff0e5`），传播感更强，但和主应用统一性略弱
- 建议方案：
  - 主功能页严格回归 v2：移除自绘导航，保留系统导航
  - 分享页采用“传播方案”：允许保留海报感，但将 CTA 按钮、tag、卡片边距、字号、颜色比例向 v2 收敛
  - 暂不建议把分享页完全改成普通 v2 内页，否则分享传播识别度会下降

---

**改动统计**：新建 11+ 个文件，修改 8+ 个文件，云函数部署 5 次，前端构建 15+ 次。约 3000+ 行新代码。未提交。

# 2026-06-08 工作总结

## 主线：订阅体系 v1→v2→v3.2 完整演进

### 一、v3.1 "次数"体系设计

- BUSINESS-MODEL-v3.1.html 生成
- 核心模块 `cloudfunctions/_shared/subscription.js`
- 3 个新云函数 + 6 个 AI 函数接入次数门控
- 前端：me.vue 次数卡片、admin.vue 订阅配置 tab、subscription.vue 套餐页
- 邀请链路 login/register 支持 inviteCode

### 二、v3.2 "平台 Token" 体系

用户否定"次数"，要求改为平台Token + 兑换倍率。

- `monthlyCalls`→`monthlyTokens`（免费30K/Pro 300K/Ultra ∞）
- `checkAndConsumeCall`→`checkTokenBalance`+`consumeTokens`（实际消耗×倍率后扣）
- `checkFeatureAccess` 功能门控接入所有 AI 函数
- 邀请/加油包 统一 Token 单位

### 三、配置源统一

- 倍率从 billing.modelPricing 读取
- 新用户赠送从 billing.welcomeTokens 读取
- 加油包复用 billing.rechargeTiers
- subscription config 只保留：试用期/套餐/分享奖励

### 四、修复

- me.vue 字段对齐（monthlyTokensUsed/extraTokens）
- 全局"次数"→"Token"文本替换（6个文件）
- 消费明细 loading 死循环修复
- 加油包去掉自动 admin confirm
- 套餐名"无限版"→"Ultra"

### 五、部署

- 14 个云函数更新部署
- 数据库配置迁移完成
- 冒烟测试 20/21，E2E 8/8

### 六、待处理

- wechatLogin 新字段对齐
- 部分 AI 函数 consumeTokens 待补
- 微信支付集成
