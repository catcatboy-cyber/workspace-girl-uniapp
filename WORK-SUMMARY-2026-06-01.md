# 2026-06-01 工作总结

## 一、商业模式文档 v2.0

基于 CloudBase 真实数据重做 Dom-Crush 定价方案：
- 从 `token_usage_records` 聚合 365 条真实调用记录，按 feature 统计实际 token 消耗
- 查出主力模型为混元 hy3-preview（91%），DeepSeek V4 Flash 仅 8%
- 真实月度消耗：轻度用户 ~33K Token、重度单 Crush ~60K Token
- 按混元定价计算月 API 成本仅 ¥0.07~0.12/用户，毛利 99%+
- 修复 v1.0 免费版功能矩阵与赠送 Token 的矛盾：免费版开放 AI 即时反馈 + 事件理解
- 输出 BUSINESS-MODEL.html（含真实数据、定价对比、功能矩阵）

## 二、本周复盘 → 14天复盘

- `weeklyReview` 云函数：日历周（7天）改为滚动 14 天窗口，与趋势面板统计对齐
- 云函数护栏提示词全部替换"本周"→"近14天"
- 前端 7 个文件 UI 文案替换：页面标题、按钮、标签、导航栏
- 数据库 `promptConfig.weeklyReview` 业务提示词同步更新
- 重编译 mp-weixin dist

## 三、微信登录获取昵称

- `wechatLogin` 云函数：接收 nickName/avatarUrl 参数，新用户创建时存储，老用户回登补充
- `login.vue`：`wx.login` 前先调用 `wx.getUserProfile` 获取微信昵称头像
- `api.ts`：缓存 nickName/avatarUrl，displayName 优先用昵称

## 四、API Key 加密存储（已回滚）

- 实现 `_shared/crypto.js`（AES-256-GCM），全链路加解密
- 部署后因 adminManage 保存触发配置回退，已全部 git 回滚 + 数据库恢复

## 五、教训

1. **部署 adminManage 前必须检查 DEFAULT 常量与用户实际配置一致**，否则后台保存会覆盖
2. **不能批量部署后让用户去后台"保存"验证**，配置 fallback 可能悄无声息地覆盖数据库
3. **改数据库配置必须用 $set 精确路径**，先读后写，不整文档覆盖
4. Windows shell 参数长度限制 ~8KB，大 JSON 需拆分批处理

---## 六、判断说明标签组重构

`explain.vue` 8 组 → 5 组，按数据流产生顺序重新组织：

| 旧（8组） | 新（5组） | 合并逻辑 |
|---|---|---|
| 最新分析标签 + 分数与证据说明 | **分析分数** | 意向/风险/证据/其他标记 4 个子分组 |
| 对象画像标签 | **人物标签** | 关系类型/基础信息/趣味标签 3 个子分组 |
| 14天趋势标签 + 本周复盘标签 | **趋势变化** | 14天意向/14天风险/波动/样本/本周复盘 5 个子分组 |
| 状态与问题标签 + 下一步动作标签 | **综合判断** | 阶段/氛围/风险信号/行动建议 4 个子分组 |
| 事件记录标签 | **事件标签** | 事件性质/事件主体 2 个子分组（去掉重复的关系类型） |

- 每组内部加灰色子标题分隔不同维度
- "最新分析标签"中与分数重复的意向/风险/证据项去重
- 添加 `ExplainItem` / `ExplainGroup` TypeScript 接口

## 七、分享功能优化

- 用 jimp 生成 500×400 自定义分享卡片图（白底黑框黄条 neo-brutalist 风格）
- `share.js` 增加 `imageUrl` 参数，全 5 个页面共用一套分享函数

## 八、小程序认证 & 分享恢复

- 完成小程序微信认证，分享功能解锁
- 开发工具可通过虚拟朋友/虚拟朋友圈预览分享卡片


**改动统计**：代码已全部回滚至 5/28。保留 BUSINESS-MODEL.html（新文件）。数据库已恢复。explain.vue 重写，share.js 修改，新增 share-card.png。
