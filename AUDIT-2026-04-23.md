# workspace-girl-uniapp 全面审计报告

**审计日期**: 2026-04-23  
**审计范围**: C:\Users\catca\.openclaw\workspace-girl-uniapp  
**对比基准**: 原版 workspace-girl + 2026-04-22 审计文档

---

## 执行摘要

### 当前状态
迁移版已完成核心功能实现，包括：
- ✅ 用户认证（登录/注册）
- ✅ 案例管理（创建/查看/删除/编辑画像）
- ✅ 时间线记录与自动重算
- ✅ 评估历史与趋势对比
- ✅ AI 设置与集成
- ✅ 云函数身份验证机制（已修复 A-001）

### 关键改进（相比 4月22日审计）
1. **身份验证已修复**: 云函数现在使用 `requireAuthenticatedUserId` 从 CloudBase 登录态解析真实 userId，不再信任前端传参
2. **重评功能已补全**: `reassess` 云函数现在会生成系统时间线记录
3. **编辑画像功能完整**: 包含画像预览和洞察功能

### 仍存在的关键问题
- **P0**: `debugUsers` 云函数仍然存在（数据泄露风险）
- **P1**: `createTimeline` 事务已禁用（数据一致性风险）
- **P1**: `getCaseDetail` 返回的 assessments 顺序与前端预期不一致
- **P2**: 时间线页面不支持首次进入时的 hash 定位

---

## 一、安全与权限边界

### ✅ A-001 [已修复] 云函数身份验证
**状态**: 已修复  
**验证**: 
- `cloudfunctions/_shared/auth.js` 实现了 `requireAuthenticatedUserId`
- 所有云函数（getCases, getCaseDetail, createCase, createTimeline, reassess, deleteCase, updateCaseProfile, getAISettings, updateAISettings）都调用此函数
- 身份从 CloudBase 登录态的 `customUserId` 或 `uid` 字段解析，不再依赖前端传参

**代码证据**:
```javascript
// cloudfunctions/_shared/auth.js:1-29
async function requireAuthenticatedUserId(app) {
  const userInfo = await app.auth().getUserInfo()
  const candidates = [
    userInfo?.customUserId,
    userInfo?.uid,
    userInfo?.userInfo?.customUserId,
    userInfo?.userInfo?.uid,
    userInfo?.user?.customUserId,
    userInfo?.user?.uid
  ]
  let userId = ''
  for (const value of candidates) {
    if (typeof value === 'string' && value.trim()) {
      userId = value.trim()
      break
    }
  }
  if (!userId) {
    const error = new Error('UNAUTHENTICATED')
    error.code = 'UNAUTHENTICATED'
    throw error
  }
  return userId
}
```

**前端配合**:
- `src/utils/api.ts` 中所有 API 调用已移除 `userId` 参数传递
- `src/utils/cloudbase.ts` 实现了 `ensureCloudAuthReady()` 确保调用云函数前已登录

---

### ❌ A-002 [未修复] debugUsers 云函数仍然存在
**等级**: P0  
**风险**: 可枚举用户邮箱和内部 ID  
**位置**: `cloudfunctions/debugUsers/`  
**建议**: 
1. 立即删除或下线部署
2. 若必须保留，添加管理员鉴权和环境限制

---

## 二、数据一致性与写入链路

### ⚠️ A-101 [部分修复] createTimeline 事务已禁用
**等级**: P1  
**现状**: 
- 代码中事务逻辑已实现但被注释掉（第 131-136 行）
- 当前为顺序写入，无原子性保证
- 异常时不会回滚

**代码证据**:
```javascript
// cloudfunctions/createTimeline/index.js:131-136
// 临时禁用事务以排查超时问题
// transaction = await db.startTransaction()
// const transactionalCase = await getOwnedCase(transaction, caseId, userId)
// if (transactionalCase.error) throw new Error(transactionalCase.error.message || 'CASE_ACCESS_DENIED')
// if (transactionalCase.caseDoc.latestResultId !== caseDoc.latestResultId) {
//   throw new Error('STALE_CASE_VERSION')
// }
```

**风险场景**:
1. 写入 timeline_records 成功
2. 写入 assessments 失败
3. 用户看到"已记录"，但评估结果未更新

**建议**:
1. 排查事务超时根因（可能是 AI 调用耗时过长）
2. 将 AI 调用移到事务外，仅事务内写数据库
3. 或实现补偿机制：失败时清理已写入的记录

---

### ✅ A-201 [已修复] reassess 现在生成系统时间线
**状态**: 已修复  
**验证**: `cloudfunctions/reassess/index.js:78-100` 调用了 `buildTrendTimelineRecords` 并写入系统记录

---

### ⚠️ A-104 [未修复] getCaseDetail 返回顺序问题
**等级**: P1  
**现象**: 
- `getCaseDetail` 返回 assessments 按 `createdAt asc` 排序（第 18 行）
- 前端多处假设"数组末尾是最新"（case-detail.vue:344, 358）
- 但 `latestResult` 优先从 `latestResultId` 查找，有一定容错

**代码证据**:
```javascript
// cloudfunctions/getCaseDetail/index.js:16-28
const { data: assessments } = await db.collection('assessments')
  .where({ caseId })
  .orderBy('createdAt', 'asc')  // 正序
  .get()

const latestResult = caseDoc.latestResultId
  ? assessments.find((item) => item._id === caseDoc.latestResultId) || assessments[assessments.length - 1] || null
  : assessments[assessments.length - 1] || null
```

```javascript
// src/pages/case-detail/case-detail.vue:344
const trend = computed(() => {
  const assessments = caseFile.value?.assessments || []
  if (assessments.length < 2) return null
  return compareAssessments(assessments[assessments.length - 2], assessments[assessments.length - 1])
  // ↑ 假设末尾是最新
})
```

**影响**: 
- 趋势对比可能错误（对比的不是最近两次）
- 评估历史页编号可能混乱

**建议**: 
- 前端统一从 `latestResult` 和 `latestResultId` 获取最新评估
- 趋势对比时按 `createdAt` 排序后再取最后两个

---

### ⚠️ A-105 [部分修复] 时间线字段模型
**状态**: 前端已做兼容处理  
**验证**: `src/utils/api.ts:27-43` 实现了 `normalizeTimelineRecord` 统一字段映射

```typescript
function normalizeTimelineRecord(record: any) {
  const fallbackDate = formatTimelineDisplayDate(record.occurrenceAt || record.createdAt)
  return {
    ...record,
    id: record.id || record._id,
    date: typeof record.date === 'string' && record.date.trim()
      ? record.date
      : typeof record.dateLabel === 'string' && record.dateLabel.trim()
        ? record.dateLabel.trim()
        : fallbackDate,
    dateLabel: typeof record.dateLabel === 'string' && record.dateLabel.trim()
      ? record.dateLabel.trim()
      : fallbackDate
  }
}
```

**残留问题**: 
- 云函数写入时使用 `dateLabel`（createTimeline:68）
- 但前端某些地方仍读 `date` 字段
- 建议统一为 `dateLabel` 作为展示字段，`occurrenceAt` 作为排序字段

---

### ✅ A-102 [已实现回滚] deleteCase 和 reassess 有错误处理
**验证**: 
- `reassess/index.js:14-31` 实现了 `rollbackReassessment` 函数
- `deleteCase` 虽无事务，但删除操作本身幂等性较强

---

## 三、页面功能完整性

### ✅ A-401 [已修复] 编辑画像页功能完整
**状态**: 已实现  
**验证**: 
- `src/pages/edit-profile/edit-profile.vue` 包含：
  - 当前画像预览（第 29-34 行）
  - 画像洞察（第 36-43 行，调用 `buildProfileInsight`）
  - 头像选择器（第 96-98 行，使用 `ProfileAvatarPicker` 组件）

---

### ⚠️ A-301 [未修复] 时间线页不支持首次进入 hash 定位
**等级**: P2  
**现象**: 
- 详情页点击焦点证据跳转 `timeline?caseId=...&targetEventId=...`（case-detail.vue:436）
- 时间线页只在 `onLoad` 后处理 `targetEventId`，但实际滚动逻辑未实现

**建议**: 
- 在 `timeline.vue` 的 `onReady` 或数据加载完成后，检查 `targetEventId` 参数
- 使用 `uni.pageScrollTo({ selector: '#event-' + targetEventId })` 滚动到目标

---

### ⚠️ A-302 [未修复] 删除成功提示的 tabBar 路由传参
**等级**: P2  
**现象**: 
- 详情页删除后使用 `uni.setStorageSync('casesDeletedFlag', '1')` + `uni.switchTab`（case-detail.vue:482-485）
- 但 cases 页面未读取此 flag

**建议**: 
- cases 页面 `onShow` 时检查 `uni.getStorageSync('casesDeletedFlag')`
- 显示提示后立即清除 flag

---

### ⚠️ A-204 [设计问题] 重评页允许编辑名称/画像但不提交
**等级**: P1  
**现象**: 
- `src/pages/reassess/reassess.vue` 使用 `AssessmentForm` 组件
- `AssessmentForm` 显示名称和画像字段
- 但提交时只发送 `answers`（reassess.vue 调用 `reassess` API）

**建议**: 
- 在重评页禁用名称/画像编辑区域
- 或拆分成"创建表单"和"重评表单"两个组件

---

## 四、UI 与体验

### ✅ A-203 [已实现] 编辑画像后的成功状态
**验证**: 
- `edit-profile.vue:224` 保存后跳转 `case-detail?caseId=...&profileUpdated=1`
- `case-detail.vue:11-14` 显示成功提示卡片

---

### ⚠️ A-501 [未统一] 密码规则
**等级**: P2  
**现象**: 
- 前端注册页提示"至少6位"
- 后端 `register` 云函数未强制校验长度
- 原版要求 8 位

**建议**: 前后端统一为至少 8 位

---

## 五、代码质量观察

### 优点
1. **认证机制健壮**: `_shared/auth.js` 实现了统一的身份验证和权限检查
2. **错误处理完善**: 云函数都有 try-catch 和错误映射
3. **前端数据归一化**: `api.ts` 对返回数据做了统一处理
4. **组件化良好**: `AssessmentForm`, `ProfileAvatarPicker`, `AssessmentTrendChart` 等组件复用性强

### 待改进
1. **事务使用**: `createTimeline` 事务被禁用，需排查根因
2. **数据顺序约定**: 前后端对 assessments 顺序理解不一致
3. **字段命名**: `date` vs `dateLabel` vs `occurrenceAt` 混用
4. **调试代码**: `debugUsers` 云函数应移除

---

## 六、修复优先级建议

### P0 - 立即修复
1. **删除 debugUsers 云函数**（数据泄露风险）

### P1 - 本周内修复
1. **恢复 createTimeline 事务**或实现补偿机制
2. **统一 assessments 顺序约定**（前后端对齐）
3. **修复重评页名称/画像编辑问题**（禁用或实现提交）

### P2 - 下个迭代
1. 时间线 hash 定位
2. 删除成功提示 flag 读取
3. 密码规则统一为 8 位
4. 字段命名统一（date/dateLabel/occurrenceAt）

---

## 七、测试建议

### 功能测试
- [ ] 创建案例 → 添加时间线 → 验证评估自动更新
- [ ] 重评 → 验证系统时间线生成
- [ ] 编辑画像 → 验证成功提示
- [ ] 删除案例 → 验证列表页提示
- [ ] 点击焦点证据 → 验证时间线定位

### 安全测试
- [ ] 尝试篡改本地 userId → 验证云函数拒绝
- [ ] 尝试访问他人 caseId → 验证返回"无权访问"
- [ ] 调用 debugUsers → 验证是否已下线

### 压力测试
- [ ] 并发创建时间线 → 验证数据一致性
- [ ] AI 调用超时场景 → 验证错误处理

---

## 八、与原版功能对比

| 功能模块 | 原版 | 迁移版 | 状态 |
|---------|------|--------|------|
| 用户认证 | ✅ | ✅ | 完整 |
| 案例管理 | ✅ | ✅ | 完整 |
| 时间线记录 | ✅ | ✅ | 完整 |
| 自动重算 | ✅ | ✅ | 完整（但事务禁用） |
| 评估历史 | ✅ | ✅ | 完整 |
| 趋势对比 | ✅ | ✅ | 完整 |
| 画像编辑 | ✅ | ✅ | 完整 |
| 画像洞察 | ✅ | ✅ | 完整 |
| AI 设置 | ✅ | ✅ | 完整 |
| 头像选择 | ✅ | ✅ | 完整 |
| 焦点事项 | ✅ | ✅ | 完整 |
| 对象状态卡 | ✅ | ✅ | 完整 |
| 娱乐洞察 | ✅ | ✅ | 完整 |
| 7天回顾 | ✅ | ✅ | 完整 |
| 时间线定位 | ✅ | ⚠️ | 部分（首次进入不支持） |
| 删除提示 | ✅ | ⚠️ | 部分（需读取 flag） |

---

## 九、结论

迁移版已完成核心功能实现，相比 4月22日审计，**关键的身份验证问题（A-001）已修复**，重评功能和画像编辑功能也已补全。

**当前阻塞上线的问题**:
1. `debugUsers` 云函数存在数据泄露风险（P0）
2. `createTimeline` 事务禁用导致数据一致性风险（P1）

**建议上线前完成**:
- 删除或下线 `debugUsers`
- 恢复 `createTimeline` 事务或实现补偿机制
- 统一前后端对 assessments 顺序的理解

完成以上修复后，项目可进入灰度测试阶段。
