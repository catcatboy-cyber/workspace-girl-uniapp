# 2026-06-27 工作日志

## 第十步（21:20）— 决定自研微信支付，完成研发计划

### 背景
CloudBase 集成中心回调链路被网关层静默阻断（handlerUnifiedTrigger 从未触发），网关层日志不对外开放，无法定位。APIv3密钥同步、公钥ID对照、回调URL连通性测试全部通过后仍然无法收到回调。决定放弃集成中心，完全自写微信支付V3接口。

### 产出
- 完成研发计划文档：`微信支付自研迁移计划.md`
- 计划核心：新增 `_shared/wxpay-v3.js` 工具库（RSA签名/AES-GCM解密/回调验签），recharge 云函数新增 `unifiedOrder`/`queryOrder` action + HTTP回调入口，前端改为 callFunction 调用
- 可直接复用：createPaymentOrder、fulfillPayment、grantRechargeTokens、fulfillSubscription
- 需要删除：scfInvokeTemplate、httpPostJSON、activeWeChatQuery、debugWxQuery
- 控制台需要配 7 个环境变量 + 1 个 HTTP 触发器

## 第九次修复（20:40）— 显式传 notify_url + 回调URL完整诊断

### 回调URL硬性要求验证（全部通过）
1. ✅ 公网可达 — 解析到 106.55.209.168，GET/POST 均响应
2. ✅ 无URL参数 — /wechatpay/order 干净路径
3. ✅ 无尾部斜杠
4. ✅ HTTPS证书有效 — DigiCert签发 *.tcloudbase.com，有效期至2026-09-27，支持TLS 1.2
5. ✅ 域名ICP备案 — tcloudbase.com 腾讯云自有域名

### 网关行为
- POST /wechatpay/order 返回 400 + `"signature verification or decryption failed: key Wechatpay-Serial is empty in header"`
- 网关活着，在正常验签，只是测试请求没带微信头被拒（正常）
- handlerUnifiedTrigger 从未触发 → 回调要么没到，要么被网关层拦截

### 新假设
CloudBase文档区分了支付回调和退款回调：
- 退款回调：模板自动塞 notify_url
- 支付回调：文档说"需同步填入商户平台支付结果通知URL"，暗示模板可能没自动塞 notify_url

前端 data 里没有 notify_url 字段，模板可能也没从环境变量 notifyURLPayURL 注入。

### 改动
在 token-recharge.vue 和 subscription.vue 的 callHTTPFunction data 里显式加入：
`notify_url: 'https://crushradar2-lruw5vn1.integration-callback.tcloudbase.com/wechatpay/order'`

### 待验证
1. 模板是否透传 notify_url 给微信
2. 如果不透传，需检查函数环境变量 notifyURLPayURL 是否设置
3. 如果回调到了但被网关拒绝，需检查微信支付公钥内容（非ID）是否正确

## 第八次修复（19:25）— 主动查单绕过回调（已废弃）

### 背景
新模板 `crushradar2-lruw5vn1-demo-scfweb` 的 ORDER（统一下单）路径正常，支付成功。但微信异步回调始终不到 recharge 云函数（两个函数日志都无回调记录），订单一直 pending。

### 根因
微信支付回调未到达 CloudBase 集成中心，可能是回调 URL 配置或网关路由问题。不再纠结回调，改为主动查单。

## 第九次排查（20:25）— 密钥同步后回调仍不到

### 已排除
- APIv3 密钥两边已同步 ✅（商户平台 + 集成中心都设了同一个值）
- 微信支付公钥ID两边一致 ✅
- 回调URL可访问 ✅（curl 测试返回网关响应）
- 下单成功 ✅（handlerUnified 正常，prepay_id 返回）
- 支付成功 ✅（商户平台显示买家已支付，有 transaction_id）

### 核心症状
- `crushradar2-lruw5vn1-demo-scfweb` 日志只有 handlerUnified（下单），从未出现 handlerUnifiedTrigger（回调）
- 说明 CloudBase 网关层在验签/解密环节拦截了微信回调，没有转发到云函数
- 或微信根本没发回调到这个URL

### CloudBase 官方FAQ排查清单
1. 回调URL配置 ✅（自动生成）
2. APIv3密钥 ✅（已同步）
3. 集成状态需确认（是否有"已激活"状态）
4. 商户平台「交易中心→通知查询」查微信侧回调下发状态（用户找不到此页面）
5. 修改配置后自动部署可能失败，需手动重新部署

### 待验证
- 修改配置后 CloudBase 自动部署是否成功
- 微信侧到底有没有发回调（需查商户平台通知状态）

### 废弃原因
QUERY 路径在旧模板上已验证返回 -9999，新模板大概率同样不可用。用户明确要求不再走 QUERY 路。代码已回退到纯轮询方案。

## 第九次排查（19:35）— 回到回调根因，查社区文档

### 核心发现（来自 CloudBase 官方文档 FAQ Q5）
回调不到的排查清单（按顺序）：
1. **微信商户平台 APIv3 密钥** — 如未设置或不一致，微信服务器**直接拒绝下发任何回调通知**。这是最可能的根因。
2. **微信商户平台「支付通知 URL」** — 需指向集成中心回调地址 `https://<标识>.integration-callback.tcloudbase.com/wechatpay/order`
3. **集成中心状态** — 需为「已激活」
4. **handlerUnifiedTrigger 日志** — `xxx-demo-scfweb` 函数日志中应出现此方法
5. **微信商户平台「交易中心→通知查询」** — 查看微信侧回调发送状态

### 关键证据
- `crushradar2-lruw5vn1-demo-scfweb` 日志只有 `handlerUnified`（下单），无 `handlerUnifiedTrigger`（回调）→ 回调根本没到集成中心
- 新旧模板都收不到回调 → 系统性问题，不是模板问题
- 最可能：APIv3 密钥未在微信商户平台设置，或与集成中心不一致

### 代码状态
- 前端已回退到纯轮询方案（30s/20轮），无 QUERY 逻辑
- 模板名：`crushradar2-lruw5vn1-demo-scfweb`

### 改动
- `token-recharge.vue` + `subscription.vue`：支付成功后先通过 `callHTTPFunction` GET `/wx-pay/wxpay_query_order_by_out_trade_no?out_trade_no={orderNo}` 主动查单
- 如果 QUERY 返回 `trade_state=SUCCESS` + `transaction_id`，传给 `recharge.queryPaymentOrder`（带 wxTradeState + wxTransactionId）直接发货
- 如果 QUERY 失败，回退到 30 秒轮询等待回调

### 关键发现
- 新模板完整 HTTP 函数名是 `crushradar2-lruw5vn1-demo-scfweb`（不是集成标识 `crushradar2-lruw5vn1`）
- `callHTTPFunction` 的 `name` 参数必须填完整函数名，不是集成标识
- `recharge.queryPaymentOrder` 已有 `wxTradeState=SUCCESS` + `wxTransactionId` 的直接发货逻辑

## 第七次修复（18:50）— 重建集成模板

### 背景
旧模板 `CrushRadar-uty6nxqu-demo-scfweb` 的 QUERY 路径硬返回 `-9999`，三种云函数内部调用方式全部失败。判断模板内部状态损坏，决定重建。

### 改动
- 用户新建集成模板 `crushradar2-lruw5vn1`（CloudBase 控制台操作）
- 前端代码更新模板名：`token-recharge.vue` + `subscription.vue`
- 编译 `npm run build:mp-weixin` 成功

### 架构
- 下单：前端 `callHTTPFunction({ name: 'crushradar2-lruw5vn1', path: '/wx-pay/wxpay_order' })`
- 回调：微信异步回调到 `recharge/paymentCallback`（集成中心回调资源 = recharge）
- 轮询：前端 20×1.5s = 30s 窗口查本地 DB 确认 paid

### 待验证
1. 新模板下单是否正常返回 prepayData
2. 微信回调是否到达 `recharge` 云函数
3. 30s 轮询能否确认 paid

## 第六次修复（18:18）— 砍掉 callHTTPFunction 查单，回归纯回调 + 加长轮询

### 现象
`callHTTPFunction` QUERY 路径 `/wx-pay/wxpay_query_order` 返回 `-9999`（不是 CHUNKED），模板函数直接拒绝。ORDER 路径能通但 QUERY 路径不可达——这是 CloudBase 集成中心模板的限制，不是 devtools bug。

### 方案
彻底移除前端 `callHTTPFunction` 主动查单，改为纯轮询等微信回调：
- 轮询窗口从 15s（10×1.5s）→ **30s（20×1.5s）**
- 每次轮询打印 `elapsed` 计时，方便判断回调到达时间
- 依赖集成中心回调（已改为 `recharge`）将订单标记为 paid

### 改动文件
- `token-recharge.vue`：移除 4a/4b callHTTPFunction 查单块，简化为纯轮询
- `subscription.vue`：同上
- 云函数 `recharge/index.js` 无需改动（wxTradeState 分支保留无害）

### 关键观察
若 30s 轮询后仍然 pending → 回调没到 recharge → 需要在 CloudBase 控制台确认：
1. 集成中心微信支付 → 下单回调资源 = `recharge`
2. `recharge` 云函数日志中是否有 `[PAYDBG][main] NO-ACTION event` 或 `TRANSACTION.SUCCESS`

### 代码现状确认

实际代码已改进（v2）：
- 前端轮询窗口加长到 15s（10 次）
- 回调日志完善（PAYDBG）
- `scfInvokeTemplate` debug 工具函数已写
- `CrashRadar`→`CrushRadar` typo 已修复

### 发现的核心问题

`queryPaymentOrder`（第 302-319 行）**仍然只查本地 DB 不主动查微信支付**。虽然 `scfInvokeTemplate` 工具函数已写好（第 397-436 行），但 `queryPaymentOrder` 完全没有调用它。

这意味着：前端支付成功后轮询 15s，如果回调没到 → 永远拿不到 paid。

### 本次修复（17:05）

**`cloudfunctions/recharge/index.js` `queryPaymentOrder`**：
- 当订单 status=pending 时，调用 `scfInvokeTemplate` 主动查微信模板 `wxpay_query_order_by_out_trade_no`
- 解析 SCF Invoke 返回的 `Result.Output` → 取 `trade_state` 和 `transaction_id`
- `trade_state === 'SUCCESS'` → 自动调 `fulfillPayment` 发货
- 查单失败则静默降级，下次轮询再试

采用纯服务端方案，无需改前端代码。前端轮询时如果回调还没到，云函数自带 fallback 主动查。

### 仍需确认（控制台）
1. CloudBase 集成中心 → 微信支付 → notify 回调目标函数是否为 `recharge`
2. 查看 `recharge` 云函数日志搜 `[PAYDBG][main] NO-ACTION event` 有无回调进来
3. 数据库 `recharge_orders` 有无 `status: paid` 的记录

### 改动文件
- `cloudfunctions/recharge/index.js`（queryPaymentOrder 增加 activeWeChatQuery fallback）

## 第四次修复（17:40）— httpPostJSON 改为前端查单 + 透传

### 现象
`httpPostJSON` 调 `https://{envId}.service.tcloudbase.com/...` 返回 `INVALID_PATH` 404。
CloudBase HTTP 访问服务对内部云函数调用有额外鉴权/路由限制，不能简单用 HTTPS 访问。

### 修复
改为「前端查微信 → 透传给云函数」方案：
1. **前端** (`token-recharge.vue`, `subscription.vue`)：支付成功后调 `wx.cloud.callHTTPFunction`（和下单同一通道）查 `/wx-pay/wxpay_query_order_by_out_trade_no`，拿到 `trade_state` + `transaction_id`
2. **轮询时**透传给 `queryPaymentOrder({ wxTradeState, wxTransactionId })`
3. **云函数** `queryPaymentOrder`：收到 `wxTradeState=SUCCESS` + `wxTransactionId` → 直接 `fulfillPayment` 发货

### 当前保障层级
- 层1：微信回调直达 `recharge/paymentCallback`（回调资源已改为 recharge）
- 层2：前端主动查微信 + 透传给云函数立即发货（轮询每轮都带）
- 前端的 `wx.cloud.callHTTPFunction` 下单已验证可用，查单共享同一通道

### 改动文件
- `cloudfunctions/recharge/index.js`（queryPaymentOrder：移除 httpPostJSON，改为收 wxTradeState/wxTransactionId）
- `src/pages/token-recharge/token-recharge.vue`（支付后主动查微信 + 透传）
- `src/pages/subscription/subscription.vue`（同上）

## 第五次修复（17:45）— 前端需重新编译

### 现象
17:45 测试支付，云函数日志显示 `wxTradeState=` 为空，说明前端主动查微信逻辑未生效。

### 根因
**前端 `.vue` 改动后没 build！** uni-app 的 Vue 源码需要编译成 `dist/build/mp-weixin/` 后微信开发者工具才能加载。用户一直跑的是旧产物。

### 修复
运行 `npm run build:mp-weixin`，确认编译产物 `dist/build/mp-weixin/pages/token-recharge/token-recharge.js` 和 `subscription.js` 已包含 `/wx-pay/wxpay_query_order_by_out_trade_no` 查单逻辑。

### ⚠️ 重要提醒
每次改 src/ 下的前端代码后必须重新 build。建议开发时用 `npm run dev:mp-weixin` 开启 watch 模式自动编译。

## 第六次修复（17:51）— 修正查单路径 + 响应解析

### 现象
build 后仍 `wxTradeState=` 为空。callHTTPFunction 查单静默失败（catch 吞掉了错误）。

### 根因
1. **路径错误**：`/wx-pay/wxpay_query_order_by_out_trade_no` 不存在
   - CloudBase 微信支付模板查单路径是 `/wx-pay/wxpay_query_order`（参数 `{ out_trade_no }`）
2. **响应解析不够深**：下单响应是 3 层 data 嵌套（`res.data.data.data`），查单解析只取了 2 层（`body.data`）

### 修复
- 路径：`/wx-pay/wxpay_query_order_by_out_trade_no` → `/wx-pay/wxpay_query_order`
- 解析：`body.data || body` → `body.data.data || body.data || body`（和下单对齐 3 层穿透）
- 日志：catch 改为 `console.error(JSON.stringify(e))`，输出完整错误信息
- 新增 `[PAYDBG] WeChat查单原始响应=` 日志，方便定位格式问题
- `token-recharge.vue` + `subscription.vue` 同步修改

## 第五次修复（17:55）— `callHTTPFunction` ERR_INVALID_CHUNKED_ENCODING

### 现象
前端 `wx.cloud.callHTTPFunction` 查单返回 `ERR_INVALID_CHUNKED_ENCODING` + `errCode:-9999`。
这是微信开发者工具 Windows 版的已知网络代理 bug，真机上不会出现。

### 修复
把查微信支付的工作从**前端**移到**云函数内部**，绕过 `callHTTPFunction`：

**云函数 `recharge/index.js` — `queryPaymentOrder`**：
- pending 时用 `app.callFunction({ name: 'CrushRadar-uty6nxqu-demo-scfweb', data: { httpMethod, path, body } })` 内部调用集成中心模板查微信
- 支持多种响应格式解析（`result.body` / `result.data` / 穿透 `data.data`）
- SUCCESS → `fulfillPayment` 直接发货

**前端 `token-recharge.vue` / `subscription.vue`**：
- 移除 `callHTTPFunction` 查单逻辑（步骤 4a）
- 改为纯轮询 `queryPaymentOrder`（云函数内部自愈）
- 轮询窗口从 10 次加到 12 次（~20s）

### 改动文件
- `cloudfunctions/recharge/index.js`（queryPaymentOrder 重写为 callFunction 查单）
- `src/pages/token-recharge/token-recharge.vue`（移除 callHTTPFunction，纯轮询）
- `src/pages/subscription/subscription.vue`（同上）

## 第六次修复（18:05）— `app.callFunction` FunctionType 无效

### 现象
云函数 `app.callFunction({ name: 'CrushRadar-uty6nxqu-demo-scfweb' })` 报错 `FunctionType parameter is invalid`。
集成中心模板函数不是标准云函数，`callFunction` 无法调用。

### 修复（方案回退到前端查 + 重试容错）
- **云函数**: `queryPaymentOrder` 回归简单查本地 DB，接受前端 `wxTradeState` + `wxTransactionId` 直接发货
- **前端**: 支付成功后用 `callHTTPFunction` 查微信（最多 3 次重试），`ERR_INVALID_CHUNKED_ENCODING` 触发重试（devtools bug 有时自愈），拿到 SUCCESS 立即传给云函数发货，查不到则轮询兜底
- 改动文件: `recharge/index.js`, `token-recharge.vue`, `subscription.vue`
- 云函数已部署 + 前端已 build

