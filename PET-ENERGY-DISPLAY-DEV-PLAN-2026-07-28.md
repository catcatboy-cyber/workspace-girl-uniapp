# 宠物活力短暂展示方案 · 2026-07-28

## 一、结论

宠物活力不常驻显示。

采用“短暂出现 + 操作后反馈 + 随时可查”的渐进披露方案：

1. 用户进入首页时短暂看到当前活力，随后自动消失。
2. 记录、聊天、帮回复、陪伴宠物成功后，短暂显示实际加分。
3. 点击宠物后，在现有“聊天”旁增加“活力”入口。
4. 点击“活力”打开详情 Sheet，查看准确分值、今日加分次数和规则。
5. 在展示分值前，先修复宠物动作与分值恢复不一致的问题。

本期不调整加分数值、每日次数上限和衰减规则，不引入等级、经验、商城或云端同步。

---

## 二、设计目标

- 让用户理解宠物为什么开心、疲惫或跑屏。
- 让每次有效操作产生清晰、即时的养成反馈。
- 保持首页关系分析为主，宠物活力为辅，不让分值长期抢占注意力。
- 精确数字只在短暂提示和详情 Sheet 中出现，不制造刷分压力。
- 所有状态使用“图标 + 文字 + 进度”表达，不只依赖颜色。
- 小咪与 Doggo 复用同一套活力规则和交互。

## 三、产品语义

统一对用户使用“活力”，代码内部可以继续沿用 `petEnergy`。

| 分值 | 状态文案 | 默认动作 |
|---|---|---|
| 80–100 | 活力充沛 | `jumping` |
| 50–79 | 状态不错 | `idle` |
| 25–49 | 有点疲惫 | `waiting` |
| 0–24 | 需要陪伴 | `failed` |

低分不使用“失败”“濒死”“没救了”等惩罚性文案。

---

## 四、展示规则

### 4.1 进入首页

仅在以下条件之一满足时展示当前活力：

- 本次应用会话首次进入首页；
- 距离上次首页活力提示已超过 30 分钟；
- 离开首页期间活力发生变化；
- 活力跨越了 25、50、80、100 任一状态边界。

普通 tab 切换、短时间内反复 `onShow` 不重复展示。

时序：

```text
首页可交互
  → 等待 600ms，避免与首屏加载抢注意力
  → 显示活力提示 3s
  → 200ms 淡出
```

文案例子：

```text
活力充沛 · 85/100
状态不错 · 65/100
需要陪伴 · 20/100
```

低于 25 分时可显示 4 秒，并使用温和提示：

```text
需要陪伴 · 20/100
和小咪聊聊，它会慢慢恢复精神
```

### 4.2 操作加分

成功加分后显示 2 秒，不显示未实际获得的奖励。

| 操作 | 提示 |
|---|---|
| 成功记录事件 | `记录完成 · 活力 +30` |
| 普通宠物聊天成功 | `陪伴成功 · 活力 +15` |
| 帮回复、主动开口、策略成功 | `一起想办法 · 活力 +10` |
| 快速三击宠物 | `陪伴成功 · 活力 +10` |

若当日该项已达上限，不显示 `+0`，改为轻提示：

```text
今天已经陪得很满足啦
```

该提示每日每个动作最多出现一次，避免反复打扰。

### 4.3 跨档反馈

加分或衰减导致状态跨档时，状态变化优先于普通加分文案：

```text
小咪恢复精神啦 · 活力充沛
小咪有点累了 · 有点疲惫
```

显示 3 秒，进度条从旧分值动画到新分值。

### 4.4 达到 100 分

达到 100 后不立即显示普通提示，进入完整奖励流程：

```text
活力满格！小咪出去跑一圈啦
  → 触发 running-right
  → 触发 running-left
  → 分值落到 85
  → 显示 2s：跑完回来啦 · 活力 85/100
```

必须明确说明跑屏会消耗 15 点活力，避免用户把 `100 → 85` 理解为系统扣错分。

30 分钟跑屏冷却期间再次达到 100：

- 分值保持 100；
- 不重复跑屏；
- 显示 `活力已满 · 休息一下再出发`；
- 冷却结束后不自动跑，下一次有效互动再触发。

---

## 五、展示优先级

活力提示不能覆盖业务结果和宠物即时反应。

优先级从高到低：

1. 额度不足、AI 错误等必须处理的反馈；
2. AI 加载、风险、正向分析等 `petScene` 临时场景；
3. 用户单击、双击、三击产生的宠物反应；
4. 活力满格跑屏；
5. 活力跨档提示；
6. 普通加分提示；
7. 进入首页时的当前活力提示；
8. 普通宠物 mood 气泡。

当高优先级场景正在展示时，活力提示进入单槽队列：

- 只保留最新一条；
- 多次加分合并，例如 `本次活力 +45`；
- Sheet 打开、宠物跑屏或宠物反应未结束时不展示；
- 页面隐藏时保留，下一次首页可交互后展示；
- 超过 5 分钟的普通加分提示自动丢弃，当前活力仍可通过详情查看。

---

## 六、首页视觉方案

### 6.1 短暂活力提示

提示使用宠物附近的小型胶囊，不占用主卡片区域，也不改变页面布局。

```text
┌────────────────────┐
│ [活力 SVG] 活力充沛 · 85/100 │
│              ████████░░ │
└────────────────────┘
```

视觉规范：

- 使用现有 Campus Pop 主题变量，不引入独立配色体系；
- 背景：`var(--surface, #fff)`；
- 文字：`var(--text-main, #111)`；
- 边框：`var(--border-width, 2rpx)`；
- 圆角：`var(--shape-radius-control, 0)`；
- 阴影：`var(--shadow-hard, 3rpx 3rpx 0 #111)`；
- 主文案：`$fs-caption` / `$fw-label`；
- 数字使用等宽数字特性，避免加分时宽度跳动；
- 进度条高 10–12rpx，使用主题强调色；
- 出现：`opacity + translateY`，200ms ease-out；
- 消失：opacity 150–200ms ease-in；
- 只动画 `transform` 和 `opacity`，不动画布局尺寸；
- 结构图标使用现有 SVG，不使用 emoji 作为功能图标。

### 6.2 点击宠物后的入口

现有宠物操作区：

```text
[聊天]
```

调整为：

```text
[聊天]  [活力]
```

- 两个按钮保持相同尺寸、边框、阴影和文字层级；
- 点击区域至少 88rpx × 88rpx；
- 按钮间距至少 16rpx；
- “活力”使用 SVG 图标；
- 点击后立即给按压反馈，不改变布局位置；
- 3 秒无操作继续按现有逻辑自动消失。

---

## 七、活力详情 Sheet

新增 `PetEnergySheet.vue`，外层结构与“本次分析”“今日行动指南”“今日的TA”保持一致。

### 7.1 内容结构

```text
宠物活力                                          ×

┌─────────────────────────────────┐
│ 活力充沛                              85 / 100 │
│ █████████████████░░░                      │
│ 再获得 15 点，小咪就会出去跑一圈             │
└─────────────────────────────────┘

今日获得
┌─────────────────────────────────┐
│ 记录事件             1 / 3          +30       │
│ 和小咪聊             2 / 3          +30       │
│ 帮回复 / 主动开口     0 / 3           +0       │
│ 陪伴小咪             1 / 2          +10       │
└─────────────────────────────────┘

活力规则
每完整 4 小时自然减少 5 点；达到 100 后，小咪会
出去跑一圈，回来时保留 85 点活力。
```

### 7.2 文案规则

- 展示准确分值和今日次数；
- 已达上限显示“今日已完成”，不使用禁用灰制造惩罚感；
- 只展示当前规则，不展示历史流水；
- 不承诺云端同步，明确为当前设备上的宠物状态；
- 不显示“为了提高分数请重复操作”等刷分导向文案。

### 7.3 可访问性

- 关闭按钮和两个宠物操作按钮具备清晰标签；
- 状态同时提供文字，不能只看颜色；
- 大字体模式下允许换行，不截断分值规则；
- 详情列表行高满足触控与阅读需求；
- 减少动态效果模式下取消位移动画，只保留短淡入淡出；
- Sheet 底部预留安全区，不被系统手势条遮挡。

---

## 八、映射修复前置项

活力可视化上线前必须先完成以下修复。

### 8.1 临时场景结束后恢复真实 mood

当前 `applyPetScene()` 定时结束后固定执行 `startPetAnim('idle')`，导致动作与当前分值不一致。

调整为统一调用：

```ts
restorePetMoodAfterScene()
```

该函数负责：

1. 清空临时场景和临时文案；
2. 重新读取最新活力；
3. 按 0/25/50/80 分界恢复正确动作和文案；
4. 如果存在排队的活力提示，再按优先级展示。

`clearPetReaction()` 同样复用该恢复函数，不再固定回到 `idle`。

### 8.2 统一满格检测

把 `checkAndRunPet()` 从零散调用点收敛到 `feedPet()` 成功后的统一结果处理中，确保所有加分来源达到 100 都能进入相同逻辑。

`onShow` 只负责结算衰减和恢复状态，不应重复消费满格奖励。

### 8.3 分析结果动作判断

保留 `risk` 事件优先级，正向跳跃至少满足：

```text
eventType !== risk
intentDelta > 0
riskDelta 未明显上升
```

若意向和风险同时明显上升，使用 `review`，不直接开心跳跃。

### 8.4 旧数据归一化

读取 `dailyCounts` 时逐项补齐：

```ts
record / chat / petting / reply
```

所有缺失值、`NaN`、负数都归一化为 0，避免旧版本数据导致每日上限失效。

---

## 九、数据结构建议

### 9.1 加分结果

将 `feedPet()` 的返回值由单一分数扩展为：

```ts
interface PetFeedResult {
  action: 'record' | 'chat' | 'reply' | 'petting'
  previousScore: number
  score: number
  bonus: number
  applied: boolean
  dailyCount: number
  dailyCap: number
  previousMood: PetMoodLevel
  mood: PetMoodLevel
  crossedMoodBoundary: boolean
  reachedFull: boolean
}
```

现有调用方即使暂时忽略返回值也不受影响；需要展示反馈的调用方使用完整结果。

### 9.2 UI 快照

新增纯函数：

```ts
getPetEnergySnapshot()
```

统一向首页和详情 Sheet 提供：

- 当前分值；
- 状态 key 与状态文案；
- 今日各动作次数、上限和已获得活力；
- 距离下一档或满格还差多少；
- 是否处于跑屏冷却。

UI 不重复定义 bonus、cap 和分值边界，避免规则漂移。

### 9.3 待展示反馈

跨页面操作使用本地存储保存一条待展示反馈：

```ts
interface PendingPetEnergyFeedback {
  type: 'gain' | 'level-change' | 'full' | 'cap'
  bonus: number
  score: number
  action?: string
  createdAt: number
}
```

首页消费后立即删除，避免重复弹出。

---

## 十、改动文件

| 文件 | 计划改动 |
|---|---|
| `src/utils/helpers.ts` | 活力数据归一化、统一 mood 分级、结构化加分结果、UI 快照、待展示反馈 |
| `src/pages/index/index.vue` | 活力提示状态机、展示优先级、恢复真实 mood、满格跑屏统一触发、增加“活力”入口 |
| `src/components/PetEnergySheet.vue` | 新增活力详情 Sheet |
| `src/components/PetSpeakSheet.vue` | 使用结构化加分结果，写入待展示反馈 |
| `src/utils/api.ts` | 记录成功后写入结构化活力反馈 |
| `tests/` | 增加活力映射、上限、衰减、反馈消费与满格触发测试 |

不改动：

- 宠物 spritesheet 和行映射；
- 云函数和数据库结构；
- 当前加分数值与每日上限；
- 当前每 4 小时衰减 5 点的规则。

---

## 十一、实施顺序

### 阶段 A：修正底层一致性

1. 补齐旧版 `dailyCounts` 归一化。
2. 抽出统一 mood 分级和状态文案。
3. 修复临时场景、抚摸反应结束后固定回 `idle`。
4. 统一 100 分跑屏触发与 100 → 85 结算。
5. 补充单元测试。

### 阶段 B：短暂反馈

1. 新增结构化 `PetFeedResult`。
2. 新增待展示反馈存储和消费逻辑。
3. 首页增加进入提示、加分提示、跨档提示和满格提示。
4. 建立与 `petScene`、反应气泡、Sheet 的优先级队列。

### 阶段 C：主动查看

1. 宠物操作区增加“活力”按钮。
2. 新建 `PetEnergySheet.vue`。
3. 接入当前分值、今日加分统计和规则说明。
4. 对齐现有三个底部 Sheet 的标题栏、背景、字号、字重、边框和动画。

### 阶段 D：验证

1. H5 小屏、大屏及横屏验证。
2. 微信小程序真机验证触摸、定时器与安全区。
3. 大字体、主题切换、减少动态效果验证。
4. 构建和回归测试。

---

## 十二、验收清单

### 分值与动作

- [ ] 0、24、25、49、50、79、80、99、100 边界映射正确。
- [ ] 临时分析动作结束后恢复当前分值对应动作，不固定回 `idle`。
- [ ] 单击、双击、三击反应结束后恢复当前分值对应动作。
- [ ] 任一加分来源达到 100 都能触发同一跑屏流程。
- [ ] 跑屏结束后分值为 85，页面有明确解释。
- [ ] 跑屏冷却期间不会重复触发。

### 加分与衰减

- [ ] 四类加分只在业务操作成功后生效。
- [ ] 每日上限严格生效，跨天正确重置。
- [ ] 达到上限后不显示虚假的 `+0`。
- [ ] 每完整 4 小时衰减 5 点，余量结算正确。
- [ ] 旧版本缺失字段不会产生 `NaN` 或无限加分。

### 展示时机

- [ ] 会话首次进入首页，600ms 后显示当前活力。
- [ ] 活力提示 3 秒后自动消失，低活力提示 4 秒后消失。
- [ ] 30 分钟内普通 `onShow` 不重复提示。
- [ ] 加分反馈显示 2 秒，跨档反馈显示 3 秒。
- [ ] 高优先级宠物场景期间活力提示排队，不覆盖业务反馈。
- [ ] 多条加分反馈能够合并，过期提示不会延迟很久后突然出现。

### 主动查看

- [ ] 点击宠物出现“聊天”和“活力”两个入口。
- [ ] 两个入口触控范围至少 88rpx × 88rpx。
- [ ] 活力详情显示准确分值、状态、进度和今日次数。
- [ ] Sheet 样式与首页其他底部 Sheet 对齐。
- [ ] 大字体模式不截断，底部内容不被安全区遮挡。
- [ ] 状态不依赖颜色单独传达。

### 工程验证

- [ ] `npm run test:regression`
- [ ] `npm run build:h5`
- [ ] `npm run build:mp-weixin`
- [ ] `git diff --check`

---

## 十三、非本期范围

- 宠物等级、成长阶段、连续签到；
- 活力商城、道具、付费恢复；
- 云端同步与多设备合并；
- 活力历史流水和图表；
- 推送用户回来“喂宠物”；
- 修改现有奖励数值、每日次数或衰减速度；
- 新增宠物动画美术资源。

这些内容应在活力短暂展示上线并观察真实使用数据后再评估。
