# 2026-06-17 工作总结

## 一、"我们"页面全面升级

### 关系雷达（5 维指标条）
- 从 8 维 AI 黑盒打分改为 5 维结构化数据归一化
- 主动性/回应度/承诺度/情绪温度/稳定性，每个公式可复算
- 样本 < 3 自动向 50 收敛，避免小样本误判
- 底层数据：timeline 语义标签 + assessments 趋势

### 信号解释卡
- 每月 3 个关键变化：升温信号/风险信号/反常信号
- 每条附证据来源（分析节点、兑现率、事件次数）
- 反常信号自动检测（如"TA 更主动但兑现没跟上"）

### 互动天平
- 中轴发散条形图设计：左侧=你，右侧=TA
- 4 组对比（主动/回应/兑现/受阻）+ 3 格摘要 + 黄色结论框
- 承诺兑现率嵌入结论文案

### 场景气泡图
- 气泡大小 = 出现次数，象限布局（线下/线上 × 高频/低频）
- 底部 chips 可快速扫读
- 仅保留场景维度，行为和结果已整合到互动天平

### 关系轨迹图
- 折线图回归原来的 CSS 方案（小程序 SVG path 不兼容）
- 事件标记可点击展开 AI 解读
- 线条/数据点/事件标记尺寸微调

### 互动标签分布
- 精简为仅展示场景标签（行为和结果与天平重复）

### 删除
- 结构风险聚焦卡片（承诺兑现率移入天平结论）
- weekly-review 废弃页面（内容已在"我们"页展示）
- case-detail-v2 废弃页面（功能已全部迁移到 case-detail）

---

## 二、Campus Pop 设计系统建立

### 字号收敛
- case-detail 从 12 种字号收敛至 7 档（48/40/32/26/24/20/18）
- 所有 21 个用户端页面统一导入 campus-pop.scss
- uni.scss + typography.scss 已有字号 token 体系

### 共享 SCSS
- 新建 `src/styles/campus-pop.scss`
- 颜色 token / 卡片 3 种 / Hero / 标签 / 按钮 / 图表容器 mixin
- `.tag-v2` `.card-v2` `.hero-block-v2` `.section-title-v2` 集中管理

### 大字体模式
- 全量字号 ×1.2 缩放（原 ~12% → 现 ~20%）

### 命理桃花对齐
- 硬编码字号全部替换为 SCSS 变量
- Hero/Card/Title 改用 mixin 引用
- 保留 fortune 子品牌视觉体系

---

## 三、首页修复

- loading 与内容互斥（修复首次进入闪现三个提示框）
- 今日桃花卡片改为整卡 `@click="goTaohua"` 标准跳转
- 按钮替换为文字 CTA，字号对齐 7 档
- `showTaohuaTeaser` 初始值改为 `true`（卡片首帧挂载）

---

## 四、Bug 修复

- `normalizeStoredSemanticTags` 中 `self_initiated` 标签遗漏修复
- 云函数超时 15/20/30s 统一提至 60s
- 8 维/5 维信号 data schema 多次迭代修正

---

## 五、拉新体系方案设计

完成完整增长方案（输出 HTML + Markdown 两份），包含：

1. 基础设施：静默登录 + 默认 Crush + 行内画像采集
2. 四条渠道：桃花人设卡 / 小咪解读 / 我们卡片 / 桃花匹配
3. 隐私匿名化：所有分享卡片强制脱敏
4. 内容获客：8 个问题模板 → 小红书/抖音/视频号
5. 越记越准：里程碑进度条 + 小咪宠物情绪联动
6. 全渠道邀请 Token 体系
7. 开发顺序（9 步）
8. 验证计划

---

## 六、提交记录

共 9 次提交，+3000/-800 行，21 个页面文件修改，3 个设计稿新增。
