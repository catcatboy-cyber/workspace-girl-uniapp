# 微信支付自研迁移研发计划

> **文档版本**: v1.1（合并 Base64 密钥方案 + 命名统一）
> **创建日期**: 2026-06-27
> **状态**: 待执行

---

## 一、项目背景

### 1.1 当前问题

小程序支付系统基于 CloudBase 集成中心微信支付模板（`crushradar2-lruw5vn1-demo-scfweb`）构建。经过一整天排查，确认：

| 环节 | 状态 | 说明 |
|------|------|------|
| 统一下单（ORDER） | ✅ 正常 | `handlerUnified` 日志正常，返回 `prepay_id` |
| 微信支付 | ✅ 正常 | 用户完成支付，微信商户平台有交易记录 |
| **异步回调** | **❌ 不可达** | `handlerUnifiedTrigger` 从未触发，回调在网关层被静默丢弃 |
| 订单发货 | ❌ 无法执行 | 回调不到 → 订单永远 `pending` |

### 1.2 根因分析

```
微信支付服务器
    │ POST 回调
    ▼
CloudBase 网关层（验签 + AES-GCM 解密）  ← ❌ 在这里失败，被静默丢弃
    │ （从未到达）
    ▼
handlerUnifiedTrigger  ← 从未触发
    │
    ▼
recharge 云函数 paymentCallback  ← 从未执行
```

**网关层是黑盒** — 日志不对外开放，无法确认是验签失败、解密失败、还是路由配置问题。已排除：
- APIv3 密钥：商户平台与集成中心已重新同步 ✅
- 微信支付公钥 ID：两边完全一致 ✅
- 回调 URL 连通性：curl 测试通过，网关存活 ✅
- 回调 URL 格式：无尾部斜杠、无参数、HTTPS 有效、已备案 ✅

**结论：集成中心网关层的黑盒问题无法从应用侧解决，必须绕过。**

### 1.3 迁移目标

完全自写微信支付 V3 接口集成，**不依赖集成中心模板**，实现：

1. **统一下单**：云函数直接调微信 V3 API，自己签名
2. **回调接收**：云函数通过 HTTP 触发器直接接收微信回调，自己验签 + 解密
3. **主动查单**：云函数直接调微信 V3 查单 API（作为回调的兜底）
4. **全链路日志可见**：每一步都有 `console.log`，不再有黑盒

---

## 二、架构对比

### 2.1 旧架构（集成中心 — 黑盒）

```
前端                         CloudBase 集成中心                    微信支付
 │                                │                                  │
 │ callHTTPFunction ────────────→ │ handlerUnified                   │
 │  (统一下单)                     │ ── POST /v3/pay/... ──────────→ │
 │ ←──── prepay_id ──────────────│ ←──── prepay_id ────────────────│
 │                                │                                  │
 │ wx.requestPayment ────────────────────────────────────────────→ │
 │ ←──── 支付成功 ──────────────────────────────────────────────── │
 │                                │                                  │
 │                                │ ←──── POST 回调 ────────────────│
 │                                │ 网关验签+解密 ❌ (静默丢弃)        │
 │                                │ handlerUnifiedTrigger (从未触发)  │
 │                                │                                  │
 │ callFunction(queryPaymentOrder)│                                  │
 │ ──→ recharge (查 DB, pending)  │                                  │
 │ ←── pending ───────────────────│                                  │
```

**问题**：网关层不可见、不可控、不可调试。

### 2.2 新架构（自研 — 全透明）

```
前端                         recharge 云函数                       微信支付
 │                                │                                  │
 │ callFunction(unifiedOrder) ──→ │ 签名 + POST /v3/pay/... ───────→│
 │                                │ ←──── prepay_id ────────────────│
 │                                │ 生成支付参数(RSA签名)             │
 │ ←──── payParams ──────────────│                                  │
 │                                │                                  │
 │ wx.requestPayment ────────────────────────────────────────────→ │
 │ ←──── 支付成功 ──────────────────────────────────────────────── │
 │                                │                                  │
 │                                │ ←──── POST 回调 ────────────────│
 │                                │ 自己验签 + AES-GCM 解密 ✅        │
 │                                │ fulfillPayment() 发货            │
 │                                │ ──→ {"code":"SUCCESS"} ────────→│
 │                                │                                  │
 │ callFunction(queryOrder) ────→ │ 签名 + GET /v3/pay/... ────────→│
 │ (轮询兜底)                      │ ←──── trade_state ──────────────│
 │                                │ 如果 SUCCESS → fulfillPayment()  │
 │ ←──── paid ───────────────────│                                  │
```

**优势**：每一步都有日志，验签失败、解密失败、路由问题全部可见。

---

## 三、现有代码资产评估

### 3.1 可直接复用（无需改动）

| 模块 | 文件 | 说明 |
|------|------|------|
| 订单创建 | `recharge/index.js` → `createPaymentOrder` | 查配置算价、写 DB，逻辑完整 |
| 发货逻辑 | `_shared/payment-fulfillment.js` → `fulfillPayment` | 幂等锁 + 充值token/订阅升级 |
| 充值发货 | `_shared/payment-fulfillment.js` → `grantRechargeTokens` | 幂等 grant，已实现 |
| 订阅发货 | `_shared/payment-fulfillment.js` → `fulfillSubscription` | 套餐升级 + 过期时间计算 |
| 价格配置 | `_shared/billing.js` → `ensureBillingSettings` | 充值档位 |
| 套餐配置 | `_shared/subscription.js` → `getSubscriptionConfig` | 订阅价格 |
| 鉴权 | `_shared/auth.js` → `requireAuthenticatedUserId` | 用户身份验证 |

### 3.2 需要新增

| 模块 | 位置 | 说明 |
|------|------|------|
| V3 签名工具 | `recharge/_shared/wxpay-v3.js`（新文件） | RSA-SHA256 签名/验签、AES-GCM 解密、支付参数生成 |
| `unifiedOrder` action | `recharge/index.js` | 调微信 V3 下单 API + 生成支付参数 |
| `queryOrder` action | `recharge/index.js` | 调微信 V3 查单 API（替代旧 `activeWeChatQuery`） |
| HTTP 回调入口 | `recharge/index.js` → `exports.main` 顶部 | 验签 + AES-GCM 解密 + 发货 |

### 3.3 需要删除（废弃代码）

| 模块 | 位置 | 说明 |
|------|------|------|
| `scfInvokeTemplate` | `recharge/index.js` | SCF Invoke 调模板 |
| `httpPostJSON` | `recharge/index.js` | HTTP 调模板端点 |
| `activeWeChatQuery` | `recharge/index.js` | 依赖模板查单 |
| `debugWxQuery` | `recharge/index.js` | 调试函数 |
| 前端 `callHTTPFunction` | `token-recharge.vue` / `subscription.vue` | 改为 `callFunction` |

---

## 四、详细实施步骤

### 阶段 1：云函数核心开发

#### 1.1 新建 `recharge/_shared/wxpay-v3.js` — 微信支付 V3 工具库

**职责**：封装所有微信支付 V3 API 的签名、验签、解密逻辑。

```
模块导出:
├── normalizePem(envValue) → PEM/B64 规范化字符串
├── signRequest(method, url, body) → Authorization header
├── wechatpayRequest(method, path, body) → 通用 HTTPS 请求封装
├── createJsapiOrder(params) → { success, prepay_id }
├── createPayParams(prepayId) → { timeStamp, nonceStr, package, signType, paySign }
├── queryOrderByOutTradeNo(outTradeNo) → { trade_state, transaction_id }
├── verifyCallbackSignature(headers, body) → boolean
└── decryptResource(resource) → JSON object
```

**技术要点**：

| 功能 | 算法 | 密钥/证书 |
|------|------|-----------|
| 请求签名 | RSA-SHA256 | 商户私钥（`WXPAY_PRIVATE_KEY`） |
| 回调验签 | RSA-SHA256 verify | 微信支付平台公钥（`WXPAY_PLATFORM_PUBLIC_KEY`） |
| 回调解密 | AES-256-GCM | APIv3 密钥（`WXPAY_API_V3_KEY`） |
| 支付参数签名 | RSA-SHA256 | 商户私钥 |

**关键代码逻辑**：

**1. PEM 密钥规范化**（`normalizePem`）：

CloudBase 环境变量不支持直接存多行 PEM 文本。采用 **Base64 编码** 存储，比 `\n` 转义更无歧义：

```javascript
function normalizePem(envValue) {
  if (!envValue) return ''
  // Base64 编码的 PEM（不含 -----BEGIN/END----- 前缀即为 base64）
  if (!envValue.includes('-----BEGIN')) {
    return Buffer.from(envValue, 'base64').toString('utf8')
  }
  // 兼容历史 \n 转义格式
  return envValue.replace(/\\n/g, '\n')
}
```

> **为什么用 Base64 而不是 `\n` 转义**：某些平台/CLI 会对 `\n` 做额外转义（`\\n` → `\n` → 丢失），导致私钥损坏。Base64 编码全程无换行符，零歧义。

**2. 请求签名**（`signRequest` + `wechatpayRequest`）：
- 签名串：`HTTP方法\n请求路径\n时间戳\n随机串\n请求体\n`
- 用商户私钥做 RSA-SHA256 签名
- 构造 `Authorization: WECHATPAY2-SHA256-RSA2048 mchid="...",nonce_str="...",timestamp="...",serial_no="...",signature="..."`
- `wechatpayRequest` 封装 `https.request`，自动签名 + 超时 8s + 统一错误处理

**3. 回调验签**（`verifyCallbackSignature`）：
- 验签串：`时间戳\n随机串\n请求体\n`
- 用微信支付平台公钥做 RSA-SHA256 verify，对比 `Wechatpay-Signature` 头

**4. 回调解密**（`decryptResource`）：
- 算法：AES-256-GCM
- Key：APIv3 密钥（32 字节）
- IV：`resource.nonce`
- AAD：`resource.associated_data`
- 密文：`resource.ciphertext`（Base64 解码后）
- 解密后得到 JSON：`{ out_trade_no, transaction_id, trade_state, ... }`

**5. 支付参数生成**（`createPayParams`）：
- `package` = `prepay_id=wx...`
- 签名串：`appId\n时间戳\n随机串\npackage\n`
- 用商户私钥签名得到 `paySign`

#### 1.2 改造 `recharge/index.js` — 新增 3 个 action

**新增 `unifiedOrder` action**（替代前端 callHTTPFunction）：

```
输入: { productType, productId/planKey, billingCycle?, priceVariant? }
流程:
  1. 鉴权 → 获取 userId
  2. 获取 openid（优先 event.openid，fallback DB users.openid）
  3. 复用 createPaymentOrder 的逻辑创建 DB 订单 → 拿到 orderNo + amountFen
  4. 调 wxpay-v3.createJsapiOrder → 微信 V3 统一下单
  5. 拿到 prepay_id
  6. 调 wxpay-v3.createPayParams(prepay_id) → 生成支付参数
  7. 如果微信下单失败，更新订单为 failed
  8. 返回 { success: true, order, payParams } 给前端
```

**新增 HTTP 回调入口**（在 `exports.main` 最顶部增加分支）：

```
判断条件: event.httpMethod === 'POST' && event.path 包含 '/pay/notify'
流程:
  1. console.log 完整 headers + body（全链路可见）
  2. 调 wxpay-v3.verifyCallbackSignature(headers, body) → 验签
  3. 验签失败 → console.error + 返回 { code: "FAIL", message: "验签失败" }
  4. 调 wxpay-v3.decryptResource(resource) → 解密
  5. 提取 out_trade_no + transaction_id
  6. 查 DB 订单 → 金额校验
  7. 调 fulfillPayment(db, order, transaction_id) → 发货
  8. 返回 { code: "SUCCESS", message: "成功" }
```

**新增 `queryOrder` action**（主动查单，轮询兜底）：

```
输入: { orderNo }
流程:
  1. 鉴权
  2. 查 DB → 如果已 paid 直接返回
  3. 调 wxpay-v3.queryOrderByOutTradeNo(orderNo) → 微信 V3 查单
  4. 如果 trade_state === 'SUCCESS' → fulfillPayment 发货
  5. 返回 { success: true, order }
```

同时保留 `confirmPayment` action（`wx.requestPayment` success = 微信已扣款的确凿信号），作为即时发货的快速通道。

#### 1.3 清理废弃代码

从 `recharge/index.js` 中删除：
- `httpPostJSON` 函数
- `scfInvokeTemplate` 函数
- `activeWeChatQuery` 函数
- `debugWxQuery` 函数和对应 action 分支

从 `queryPaymentOrder` 中删除主动查微信的 fallback 逻辑（第 329-342 行），简化为纯 DB 查询。保留 `queryPaymentOrder` 兼容旧调用方。

---

### 阶段 2：前端改造

#### 2.1 改造 `src/pages/token-recharge/token-recharge.vue`

**删除**：第 107-139 行的 `wx.cloud.callHTTPFunction` 整段代码

**替换为**：

```typescript
// 调用 recharge 云函数统一下单
const orderResult = await callFunction('recharge', {
  action: 'unifiedOrder',
  productType: 'recharge',
  productId: planId
})
if (!orderResult?.success) {
  orderMessage.value = orderResult?.message || '创建订单失败'
  return
}
const payParams = orderResult.payParams
```

**支付参数直接使用**（不再需要穿透两层 data）：

```typescript
wx.requestPayment({
  timeStamp: payParams.timeStamp,
  nonceStr: payParams.nonceStr,
  package: payParams.package,
  signType: payParams.signType,
  paySign: payParams.paySign,
  ...
})
```

**轮询改为 `queryOrder`**：

```typescript
const confirm = await callFunction('recharge', {
  action: 'queryOrder',
  orderNo
})
```

#### 2.2 改造 `src/pages/subscription/subscription.vue`

同样的改动模式，将 `callHTTPFunction` 替换为 `callFunction('recharge', { action: 'unifiedOrder', ... })`。

#### 2.3 改造 `src/utils/api.ts`

新增 API 封装函数：

```typescript
export function unifiedOrder(params: {
  productType: 'recharge' | 'subscription'
  productId?: string; planKey?: string
  billingCycle?: 'monthly' | 'annual'; priceVariant?: 'standard' | 'student'
}) {
  return callFunction('recharge', { action: 'unifiedOrder', ...params, ...getBusinessAuthPayload() })
    .then((res: any) => res.result)
}

export function queryOrder(params: { orderNo: string }) {
  return callFunction('recharge', { action: 'queryOrder', ...params, ...getBusinessAuthPayload() })
    .then((res: any) => res.result)
}
```

---

### 阶段 3：控制台配置

#### 3.1 配置云函数环境变量

CloudBase 控制台 → 云函数 → `recharge` → 配置 → 环境变量：

| 变量名 | 值 | 来源 |
|--------|-----|------|
| `WXPAY_APPID` | `wxb8bd1a6b518e931e` | 集成中心配置页 / 微信开放平台 |
| `WXPAY_MCHID` | `1746982729` | 集成中心配置页 / 微信支付商户平台 |
| `WXPAY_SERIAL_NO` | （证书序列号） | 集成中心配置页 / 商户平台 |
| `WXPAY_PRIVATE_KEY` | （PEM 私钥的 **Base64 编码**） | 商户平台下载的商户 API 证书私钥 → base64 |
| `WXPAY_API_V3_KEY` | （32位密钥） | 与商户平台同步的 APIv3 密钥 |
| `WXPAY_PLATFORM_PUBLIC_KEY` | （PEM 公钥的 **Base64 编码**） | 商户平台下载的微信支付平台公钥 → base64 |
| `WXPAY_NOTIFY_URL` | （见 3.2） | HTTP 触发器地址 |

**PEM 转 Base64 命令**：
```bash
# 私钥
base64 -w0 apiclient_key.pem   # 输出到终端，复制到环境变量

# 平台公钥
base64 -w0 wechatpay_public.pem
```

在 Windows 上：
```powershell
[Convert]::ToBase64String([IO.File]::ReadAllBytes("apiclient_key.pem"))
```

#### 3.2 配置 HTTP 触发器

CloudBase 控制台 → 云函数 → `recharge` → 触发器 → 新建触发器：

| 配置项 | 值 |
|--------|-----|
| 触发器类型 | HTTP 触发器 |
| 路径 | `/pay/notify` |
| 方法 | POST |
| 鉴权 | 免鉴权（微信回调不带登录态） |

创建后获得回调 URL，格式类似：
```
https://cloud1-d0gvhqu2c8a2b61fd.service.tcloudbase.com/pay/notify
```

将此 URL 设为 `WXPAY_NOTIFY_URL` 环境变量，并用于统一下单请求的 `notify_url` 字段。

同时将此 URL 配置到微信支付商户平台「支付结果通知地址」。

#### 3.3 更新 cloudbaserc.json

```json
{
  "name": "recharge",
  "timeout": 60,
  "envVariables": {
    "WXPAY_APPID": "",
    "WXPAY_MCHID": "",
    "WXPAY_SERIAL_NO": "",
    "WXPAY_PRIVATE_KEY": "",
    "WXPAY_API_V3_KEY": "",
    "WXPAY_PLATFORM_PUBLIC_KEY": "",
    "WXPAY_NOTIFY_URL": ""
  },
  "runtime": "Nodejs18.15",
  "memorySize": 128,
  "handler": "index.main",
  "installDependency": true
}
```

> 环境变量值留空占位，实际值在 CloudBase 控制台填写（避免敏感信息进入 git）。

#### 3.4 无需新增 npm 依赖

RSA 签名、AES-GCM 解密均使用 Node.js 内置 `crypto` 模块，不需要第三方库。`cloudfunctions/recharge/package.json` 无需改动。

#### 3.5 域名白名单

确认 `api.mch.weixin.qq.com` 已在云函数的出站白名单中（CloudBase 云函数默认无出站限制，通常不需要额外配置）。

---

### 阶段 4：部署与测试

#### 4.1 部署云函数

```bash
tcb fn deploy recharge --env cloud1-d0gvhqu2c8a2b61fd
```

#### 4.2 编译前端

```bash
npm run build:mp-weixin
```

#### 4.3 测试矩阵

| 测试场景 | 预期结果 | 验证方法 |
|---------|---------|---------|
| 充值 0.10 元 | 支付成功 → 秒到账 | 看 `recharge` 日志有 `fulfill` |
| 订阅升级 0.10 元 | 支付成功 → 权益生效 | 看 `recharge` 日志有 `fulfill` |
| 支付后取消 | 订单保持 pending | `wx.requestPayment` fail 回调 |
| 回调验签失败 | 日志记录 + 返回 FAIL | 手动构造错误签名 POST |
| 回调解密失败 | 日志记录 + 返回 FAIL | 手动构造错误密文 POST |
| 重复回调 | 幂等，不重复发货 | `fulfillPayment` 幂等锁 |
| 轮询兜底 | 回调延迟时 30 秒内通过查单发货 | 断网测试 |
| 金额篡改 | 拒绝发货 | 手动改 DB 金额 |

#### 4.4 关键日志检查点

测试时在 `recharge` 云函数日志中依次确认：

```
[WXPAY][unifiedOrder] 开始统一下单 orderNo=PAY...
[WXPAY][unifiedOrder] 微信返回 prepay_id=wx...
[WXPAY][unifiedOrder] 生成支付参数完成

--- 用户支付完成 ---

[WXPAY][callback] 收到回调 headers=... body=...
[WXPAY][callback] 验签结果: true
[WXPAY][callback] 解密成功 out_trade_no=PAY... transaction_id=4200...
[WXPAY][callback] 订单匹配 amountFen=10
[PAYDBG][fulfill] mark paid orderId=...
[PAYDBG][fulfill] recharge granted / subscription fulfilled
[WXPAY][callback] 返回 SUCCESS
```

**如果某一步缺失或失败，日志会明确指出卡在哪里 — 这就是自研方案的核心优势。**

---

## 五、文件改动清单

### 5.1 新增文件

| 文件 | 说明 |
|------|------|
| `cloudfunctions/recharge/_shared/wxpay-v3.js` | 微信支付 V3 工具库（签名/验签/解密） |

### 5.2 修改文件

| 文件 | 改动内容 |
|------|---------|
| `cloudfunctions/recharge/index.js` | 新增 `unifiedOrder`/`queryOrder` action + HTTP 回调入口；删除 `scfInvokeTemplate`/`httpPostJSON`/`activeWeChatQuery`/`debugWxQuery`；简化 `queryPaymentOrder` |
| `cloudbaserc.json` | 新增 7 个 WXPAY_* 环境变量占位 |
| `src/pages/token-recharge/token-recharge.vue` | `callHTTPFunction` → `callFunction('recharge', {action:'unifiedOrder'})`；轮询改 `queryOrder` |
| `src/pages/subscription/subscription.vue` | 同上 |
| `src/utils/api.ts` | 新增 `unifiedOrder`/`queryOrder` 封装函数 |

### 5.3 不改动的文件

| 文件 | 原因 |
|------|------|
| `_shared/payment-fulfillment.js` | 发货逻辑完整，直接复用 |
| `_shared/billing.js` | 价格配置逻辑完整 |
| `_shared/subscription.js` | 套餐配置逻辑完整 |
| `_shared/auth.js` | 鉴权逻辑完整 |

---

## 六、风险与应对

### 6.1 技术风险

| 风险 | 概率 | 影响 | 应对 |
|------|------|------|------|
| RSA 签名实现有误 | 中 | 下单失败 | 先用 Postman 验证签名逻辑，再部署 |
| AES-GCM 解密实现有误 | 中 | 回调失败 | 单元测试：用已知密文+密钥验证解密结果 |
| HTTP 触发器格式与预期不符 | 中 | 回调路由不到 | 先用 curl POST 测试触发器，确认 event 结构 |
| 环境变量 Base64 解码失败 | 低 | 签名失败 | `normalizePem` 同时兼容 Base64 和 `\n` 转义格式 |
| 微信平台公钥轮换 | 低 | 回调验签失败 | 监控微信支付公告；保留手动发货入口 |

### 6.2 业务风险

| 风险 | 概率 | 影响 | 应对 |
|------|------|------|------|
| 迁移期间旧订单无法发货 | 低 | 用户投诉 | 保留 `adminConfirmRecharge` 手动发货入口 |
| 迁移期间支付不可用 | 低 | 收入损失 | 在低峰期迁移；保留旧模板作为回滚方案 |

### 6.3 回滚方案

如果自研方案上线后出现严重问题：

1. **前端回滚**：将 `callFunction` 改回 `callHTTPFunction`（旧模板仍在）
2. **云函数回滚**：保留旧版 `index.js` 的 git commit，一键 revert
3. **手动发货兜底**：`adminConfirmRecharge` action 始终可用，管理员可在后台手动确认订单

**回滚步骤**：
```bash
# 前端
git revert <commit-hash>
npm run build:mp-weixin

# 云函数
git revert <commit-hash>
tcb fn deploy recharge --env cloud1-d0gvhqu2c8a2b61fd
```

---

## 七、实施时间线

| 阶段 | 内容 | 产出 |
|------|------|------|
| **P1** | 开发 `wxpay-v3.js` 工具库 | 签名/验签/解密函数 |
| **P2** | 改造 `recharge/index.js` | 新增 3 个 action + 清理废弃代码 |
| **P3** | 改造前端两个页面 + api.ts | `callFunction` 替换 |
| **P4** | 控制台配置环境变量 + HTTP 触发器 | 回调 URL 可用 |
| **P5** | 部署 + 测试 | 0.10 元端到端验证 |
| **P6** | 全量验证 | 充值 + 订阅两种场景通过 |

**P1-P3 为代码开发，P4 的控制台配置可与之并行进行。**

---

## 八、验收标准

### 8.1 功能验收

- [ ] 充值 0.01 元 → 支付成功 → Token 秒到账
- [ ] 订阅升级 0.01 元 → 支付成功 → 权益立即生效
- [ ] 回调日志完整可见（`[WXPAY][callback]` 全链路）
- [ ] 轮询兜底：回调延迟时 30 秒内通过查单发货
- [ ] 幂等：重复回调不重复发货
- [ ] 金额校验：篡改金额的订单被拒绝

### 8.2 日志验收

- [ ] `unifiedOrder` 日志含 orderNo + prepay_id
- [ ] 回调日志含 headers + 验签结果 + 解密结果 + 发货结果
- [ ] 失败场景有明确错误日志（验签失败/解密失败/金额不匹配）

### 8.3 性能验收

- [ ] 统一下单响应 < 2 秒
- [ ] 回调处理 < 1 秒
- [ ] 轮询查单 < 1 秒

---

## 九、附录

### 9.1 微信支付 V3 API 参考

| API | 方法 | URL |
|-----|------|-----|
| JSAPI 统一下单 | POST | `https://api.mch.weixin.qq.com/v3/pay/transactions/jsapi` |
| 查单（商户订单号） | GET | `https://api.mch.weixin.qq.com/v3/pay/transactions/out-trade-no/{out_trade_no}?mchid={mchid}` |
| 支付回调通知 | POST | `{notify_url}`（由下单时指定） |

### 9.2 签名算法

```
签名串构造:
  HTTP请求方法\n
  URL请求路径（含query）\n
  时间戳\n
  随机串\n
  请求报文主体（GET 请求为空串）\n

签名:
  merchantPrivateKey.sign(signMessage, SHA256withRSA)
  Base64.encode(signature)

Authorization 头:
  WECHATPAY2-SHA256-RSA2048 mchid="商户号",nonce_str="随机串",timestamp="时间戳",serial_no="证书序列号",signature="签名"
```

### 9.3 回调解密算法

```
算法: AES-256-GCM
Key:  APIv3密钥（32字节 UTF-8 字符串）
IV:   resource.nonce
AAD:  resource.associated_data
密文: Base64.decode(resource.ciphertext)

解密后得到 JSON:
  { out_trade_no, transaction_id, trade_type, trade_state, trade_state_desc, success_time, amount, ... }
```

### 9.4 环境变量 PEM 存储方案

微信商户私钥和微信支付平台公钥都是 PEM 格式文件。环境变量用 **Base64 编码** 存储，运行时由 `normalizePem` 解码：

```javascript
function normalizePem(envValue) {
  if (!envValue) return ''
  // Base64 编码路径（推荐）
  if (!envValue.includes('-----BEGIN')) {
    return Buffer.from(envValue, 'base64').toString('utf8')
  }
  // 兼容历史 \n 转义路径
  return envValue.replace(/\\n/g, '\n')
}
```

> **选择 Base64 的原因**：CloudBase 各入口（CLI、控制台、SDK）对换行符 `\n` 的处理不一致，有些会二次转义导致私钥损坏。Base64 全程无换行，零歧义。`normalizePem` 同时兼容两种格式，降低迁移风险。

---

## 十、关键决策记录

| 决策 | 选择 | 理由 |
|------|------|------|
| 是否使用第三方支付 SDK | **否** | Node.js crypto 内置 RSA + AES-GCM，无需引入额外依赖，减少供应链风险 |
| 回调入口是否独立云函数 | **否** | 复用 `recharge` 云函数，通过 HTTP 触发器在 `main` 入口分流，减少部署复杂度 |
| PEM 密钥存储格式 | **Base64 编码** | 避免 CloudBase 各入口对换行符处理不一致导致的私钥损坏 |
| 是否保留集成中心旧模板 | **是** | 作为回滚方案保留，不删除，但不再调用 |
| 是否保留前端轮询 | **是** | 作为回调的兜底方案，确保用户体验 |
| 是否保留 `confirmPayment` action | **是** | `wx.requestPayment` success 回调是微信已扣款的确凿信号，可作为即时发货的快速通道 |
| 是否保留 `adminConfirmRecharge` | **是** | 管理员手动发货的最后防线 |
| `queryPaymentOrder` 处理 | **简化为纯 DB 查询** | 主动查单逻辑移到新的 `queryOrder` action，职责更清晰 |
