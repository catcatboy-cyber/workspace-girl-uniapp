# 微信小程序主包整改计划

日期：2026-07-28

项目：`workspace-girl-uniapp`

目标：优先用低风险方式解决微信上传错误 `80051`，恢复并保护正常业务页面，清理确认无效的资源和代码。分包改造因涉及分享路径和页面迁移，本轮暂缓执行。

## 1. 当前基线

### 1.1 当前体积

| 项目 | 当前值 |
| --- | ---: |
| `dist/build/mp-weixin` 原始文件总量 | 约 1360.6KB |
| 排除重复 `logo.png`、`app-icon.png` 后 | 约 1237.3KB |
| 微信开发者工具报出的上传源代码体积 | 2081KB |
| 微信主包限制 | 2048KB |
| 当前超限 | 约 33KB |

说明：磁盘目录体积与开发者工具上传体积不同，是因为开发者工具还会进行 ES6 转译、代码处理和打包计算。整改不能只看文件夹体积，最终以微信开发者工具“上传”前的代码体积为准。

### 1.2 已完成的止血措施

以下两个文件内容完全相同，且小程序编译产物没有引用：

- `static/logo.png`：约 61.7KB
- `static/app-icon.png`：约 61.7KB

已在 `src/manifest.json` 与根目录 `project.config.json` 中加入微信打包忽略规则，正式构建产物已经正确生成对应 `packOptions.ignore`。正式后处理脚本还会物理移除产物中的 `logo.png`；对应 ignore 条目作为构建链变化时的防守规则继续保留。

相对 2081KB 报错基线，当前新鲜正式产物中可以确认新增排除的是 `app-icon.png` 约 61.7KB 和无运行时引用静态副本约 6.1KB，理论上传体积约 2013KB；如果报错基线当时还包含 `logo.png`，则可能进一步降至约 1952KB。实际值受当时构建方式和开发者工具处理影响，必须以重新构建和真实上传为准。本轮接受约 35–96KB 的临时余量，分包作为后续单独立项，不在本轮实施。

## 2. 审计结论

### 2.1 主要体积来源

| 内容 | 体积 | 判断 |
| --- | ---: | --- |
| 全部页面代码 | 约 693.7KB | 最大结构问题，28 个页面全部位于主包 |
| JavaScript | 约 499.8KB | 大部分有效，主要来自页面逻辑和公共规则模块 |
| WXSS | 约 330KB | 首页、桃花页、关系详情占比较高 |
| PNG | 约 218.6KB | 两只宠物图片约 92.4KB；两个重复 Logo 已排除 |
| SVG | 约 103.7KB | 包含动态表情图标和部分重复静态副本 |
| WXML | 约 199.4KB | 桃花页、关系详情和首页较大 |

最大的页面：

- 首页：约 114KB
- 桃花页：约 108.5KB
- 关系详情：约 78.3KB
- 时间线：约 49.3KB
- 我的：约 39.6KB

最大的公共模块：

- `common/vendor.js`：约 65.7KB
- `utils/insights.js`：约 44.1KB
- `utils/taohua.js`：约 29.7KB
- `utils/theme.js`：约 14.4KB
- `utils/api.js`：约 12.2KB

完整引用审计没有发现可以直接删除的公共工具模块或未被调用的导出函数。`insights.js` 和 `taohua.js` 虽然较大，但仍被多个核心页面使用，不列入直接删除范围。

### 2.2 确认可从上传包排除的静态副本

除两个重复 Logo 外，正式产物中还有约 6.1KB 文件没有任何运行时引用：

- 整个旧 `static/tabbar` 图片目录，共 10 张图片
- `needle-flower.svg`
- `stars.svg`
- `needle-heart.svg`
- `heart.svg`
- `list-checks.svg`
- `book-open.svg`
- `share-2.svg` 的 `static` 副本
- `alert-triangle.svg`
- `needle-star.svg`
- `check-circle.svg`

注意：部分图标功能仍在使用，但 Vue 编译后引用的是 `/assets/文件名.hash.svg`。这里要排除的是 `static` 目录里的重复副本，不能删除编译生成的 `/assets` 文件。

### 2.3 确认无源码引用的旧组件

完整源码扫描没有发现以下组件被任何页面或组件导入：

| 文件 | 源码体积 | 对上传包的影响 |
| --- | ---: | --- |
| `AssessmentTrendChart.vue` | 约 8.8KB | 未进入构建产物，无减包收益 |
| `ProfileInline.vue` | 约 5.6KB | 未进入构建产物，无减包收益 |
| `CaseProfileFields.vue` | 约 5.2KB | 未进入构建产物，无减包收益 |
| `ProfileAvatar.vue` | 约 1.6KB | 未进入构建产物，无减包收益 |

这些文件属于仓库卫生问题，不是本次上传失败的原因。为减少风险，应放在最后阶段清理。

### 2.4 疑似遗留页面

以下页面只在 `pages.json` 注册，完整编译产物中没有任何内部页面跳转到它们：

| 页面 | 体积 | 风险判断 |
| --- | ---: | --- |
| `pages/assessments/assessments` | 约 27.9KB | 很可能已被关系详情、时间线取代 |
| `pages/register/register` | 约 5.2KB | 当前登录页没有注册入口，但可能存在邀请链接或历史直达链接 |

不能仅凭“内部引用为 0”直接删除。删除前必须确认：

1. 是否存在历史二维码、小程序消息、公众号菜单或客服话术直达该路径。
2. 是否存在云函数、数据库配置或运营后台动态下发该路径。
3. 是否需要兼容旧分享卡片。
4. 邮箱注册是否仍作为微信登录失败时的兜底方式。

## 3. 整改目标与验收线

### 3.1 必须达到

- 微信开发者工具可以正常预览、真机调试和上传。
- 不再出现错误码 `80051`。
- 主包经开发者工具处理后的实际上传体积必须低于 2048KB。
- 本轮建议至少保留约 30KB 临时余量；如可以正常上传但余量偏小，记录实际值并停止扩大改动范围。
- 正式构建必须完整执行项目后处理脚本，不能以绕过后处理的直接构建产物作为上传产物。
- 6 个受保护分享落地页全部恢复并保持可用。
- 现有分享路径、邀请参数、Tab 页面和登录重定向全部可用。

长期目标仍为主包不高于 1700KB、保留至少 300KB 安全余量。该目标依赖后续单独评审的分包或其他结构优化，不作为本轮上线门槛。

### 3.2 不作为本轮首要目标

- 不为追求几 KB 收益大规模重写业务逻辑。
- 不增加 `subPackages`，不移动页面目录，不修改分享落地路径。
- 不直接压缩或替换宠物图片，除非完成安全忽略后仍然无法上传。
- 不重构 `insights.js`、`taohua.js` 的规则算法，避免引入分析结果回归。
- 不删除外部入口尚未确认的页面。

## 4. 分阶段执行方案

## 阶段 0：基线冻结与受保护页面确认

目标：在任何忽略或清理操作前固定一个可构建、可回滚的起点，避免把工作区原有改动误判成整改结果。

### 0.1 冻结整改基线

- 记录整改起点 Commit、分支名、构建时间和微信开发者工具版本。
- 整理当前未提交改动；与本次减包无关的修改不得混入整改提交。
- 基线构建必须使用完整正式构建流程，不能使用残留的旧 `dist` 目录代替构建结果。
- 自动核对 `pages.json` 中每个微信端注册页面均存在对应源码文件。
- 基线状态下必须通过微信小程序构建和核心页面冒烟；基线本身失败时不得开始阶段 A。

### 0.2 受保护的分享落地页

以下 6 个页面均为正常业务页面或分享落地页，不属于死页面，禁止删除：

- `pages/monthly-review/monthly-review`
- `pages/quick-read/quick-read`
- `pages/pair-onboarding/pair-onboarding`
- `pages/taohua-pair-result/taohua-pair-result`
- `pages/taohua-pair-share/taohua-pair-share`
- `pages/taohua-persona-result/taohua-persona-result`

当前工作区中如有这些页面的删除状态，必须先恢复并确认内容完整，再进入阶段 A。恢复操作单独处理，不与分包迁移混在同一提交中。

本轮这 6 个页面必须保持现有注册路径和业务行为，不移动目录、不改为分包。后续如重新批准分包，仍必须满足：

- 原有分享卡片、二维码、聊天消息和历史链接仍可打开。
- 原有查询参数名称、编码方式和缺省行为保持兼容。
- 如新分包路径无法与旧路径一致，必须保留轻量兼容落地页，校验并透传全部参数后跳转。
- 未登录、已登录、资料未完成三种状态均能完成回流，不产生重定向循环。

### 阶段 0 验收

- `git status` 中不存在未确认的受保护页面删除。
- `pages.json` 注册页存在性检查通过。
- H5 与微信小程序基线构建通过。
- 6 个受保护页面逐一完成直接访问和带参数访问。
- 保存基线包体积、页面清单和构建产物摘要，作为后续每阶段对比依据。

## 阶段 A：安全止血与包规则固化

目标：确保当前版本可以上传，并保证正式构建每次都自动应用忽略规则。

### A0. 固化正式构建链

- 正式微信产物统一使用 `npm.cmd run build:mp-weixin`。
- 该命令必须成功执行 `scripts/postbuild-mp-weixin.cjs`；先解决该脚本当前的读取/执行权限问题。
- 后处理完成后核对压缩设置、`privacy.json`、自定义 TabBar、桃花图标和 DCloud 外链阴影替换结果。
- `npx.cmd uni build -p mp-weixin` 仅可用于定位编译问题，不能把绕过后处理的产物用于上传。

### A1. 固化现有重复 Logo 忽略规则

- 保留 `src/static/logo.png` 和 `src/static/app-icon.png`，避免影响 App/H5 构建。
- 仅在微信小程序 `packOptions.ignore` 中排除。
- 同时维护：
  - `src/manifest.json`
  - 根目录 `project.config.json`
- 每次生产构建后检查 `dist/build/mp-weixin/project.config.json`，确认规则没有被覆盖。

### A2. 排除确认无引用的 6.1KB 静态副本

- 优先使用 `packOptions.ignore`，暂不删除源文件。
- 构建后逐项确认：
  - `/assets/*.hash.svg` 仍存在。
  - 运行时不存在指向被忽略 `/static/...` 文件的路径。
  - 首页、三个分析弹窗、行动指南、桃花页、十二生肖/星座图标正常显示。

### A3. 上传验证

- 微信开发者工具必须打开 `dist/build/mp-weixin`，不能使用旧的 `dist/dev/mp-weixin`。
- 清除全部缓存并重新编译。
- 在“代码依赖分析/代码质量”中记录主包体积。
- 由人工确认版本号和备注后上传体验版，记录最终体积；本阶段不提交审核、不发布正式版。

### 阶段 A 回滚

- 如果任何图标缺失，只回退对应的 ignore 条目。
- 不回退已经验证安全的两个重复 Logo 规则。
- 不删除任何源资源，因此回滚只涉及配置文件。

## 阶段 B：主包分包改造（暂缓，本轮禁止执行）

风险判断：分包会同时影响页面物理目录、内部跳转、分享卡片、二维码、登录重定向和历史直达路径。当前 6 个受保护页面又承担分享落地职责，因此本轮不实施分包。

本轮明确禁止：

- 不在 `pages.json` 增加或修改 `subPackages`。
- 不移动任何页面目录。
- 不修改现有页面 URL、分享路径和分享参数。
- 不新增旧路径兼容跳转页。
- 不以“准备分包”为由删除、合并或改写页面。

分包必须在本轮上传问题解决后单独立项，重新完成路径映射、外部入口盘点、原型验证和专项测试，获得再次确认后才能实施。

<!--
暂缓的原分包设想，仅作为后续审计线索，本轮不执行：

- 桃花功能候选：taohua、taohua-share、taohua-pair-share、pair-onboarding、taohua-pair-result、taohua-persona-result。
- 分析工具候选：assessments、quick-read、monthly-review、reassess、new。
- 画像与支持候选：self-profile、edit-profile、explain、references、custom-pet、feedback、about。
- 账户与付费候选：subscription、token-usage、token-recharge。
- register 仅作为兼容页面评估，不授权迁移或删除。

后续重启分包前必须先形成逐页的 root、旧路径、新路径、参数、外部入口和兼容策略映射表。
-->

## 阶段 C：遗留页面确认（本轮只审计，不改页面）

本轮只确认 `assessments`、`register` 的真实入口和业务用途，不删除、不迁移、不增加兼容跳转。调查结果记录到后续任务，不作为本轮上传的阻塞项。

### C1. `assessments` 页面

- 盘点内部入口、历史分享、二维码、公众号、客服和运营配置。
- 记录是否仍承担分析记录入口。
- 无论调查结论如何，本轮保持页面代码和路径不变。

### C2. `register` 页面

- 确认当前生产环境是否仍支持邮箱密码登录。
- 确认邀请链接是否会直达注册页。
- 确认 H5 是否仍需要邮箱注册。
- 本轮保持页面代码、条件编译和路径不变。

### C3. 调查验收

- 形成入口来源清单和参数清单。
- 不产生任何 `pages.json`、路由或页面代码改动。

## 阶段 D：仓库死代码清理

目标：改善维护性，不作为上传问题的前置条件。

### D1. 删除前再次确认

对四个疑似死组件执行：

- 完整源码搜索。
- H5、小程序、App 三端生产构建。
- 检查是否被动态组件名、easycom 或配置文件引用。

### D2. 清理方式

- 一次只删除一组确定无引用组件。
- 删除后执行全量回归和三端构建。
- 不顺手清理其他看起来“旧”的文件，避免扩大变更范围。

## 阶段 E：深度优化（本轮暂缓）

页面样式合并、规则模块拆分和图片格式替换都可能引入视觉或业务回归，本轮不执行。仅当完成阶段 A 后仍无法上传时，才单独评估无损图片压缩，并在修改前再次确认。

### E1. 页面样式体积

重点审计：

- 首页 WXSS：约 38.6KB
- 桃花页 WXSS：约 38.1KB
- 关系详情 WXSS：约 33.2KB

只记录重复样式位置和理论收益，不移动样式、不修改作用域。

### E2. 规则模块拆分

- 本轮禁止拆分 `insights.js` 和 `taohua.js`。
- 不改变任何算法阈值、导出接口或文案。

### E3. 图片优化

- 正常上传成功时不修改宠物图片。
- 如仍无法上传，只允许先做无损压缩评估，不改尺寸、不改格式、不改透明通道。
- 实施前后必须逐像素/逐帧检查透明背景和精灵尺寸，并单独形成可回滚提交。

## 5. 完整验证矩阵

## 5.1 自动化检查

每个阶段必须执行：

```text
npm.cmd run test:regression
npm.cmd run build:h5
npm.cmd run build:mp-weixin
```

宠物能量功能及测试文件形成完整提交后补充：

```text
npm.cmd run test:pet-energy
```

条件允许时再补充：

```text
npm.cmd run build:app
```

同时执行：

- `git diff --check`
- 校验 `app.json` 的 `pages` 和 Tab 页面，确认本轮没有新增 `subPackages`。
- 校验 `project.config.json` 的 `packOptions.ignore`。
- 校验正式后处理脚本已经完成压缩设置、隐私文件、自定义 TabBar、桃花图标和外链阴影处理。
- 统计主包原始体积和微信开发者工具处理后的实际上传体积。
- 搜索构建产物中的失效路由和缺失静态文件引用。

## 5.2 微信端冒烟路径

### 启动与登录

- 冷启动
- 已登录启动
- 未登录启动
- 邀请码启动
- 资料未完成时进入本人画像
- 带 redirect 参数启动

### 五个 Tab

- 今日
- 我们
- Crushes
- 往事
- 我

### 核心关系流程

- 新建 Crush
- 快速记录
- AI 分析完成
- 本次分析弹窗
- 行动指南弹窗
- 今日的 TA 弹窗
- 时间线
- 重新分析
- 月度复盘

### 桃花流程

- 桃花首页
- 桃花人格分享
- 配对 onboarding
- 配对结果
- 配对分享
- 人设结果
- 分享卡片回流

### 账户和个人资料

- 本人画像
- 编辑 Crush 画像
- 订阅
- Credits 充值
- Credits 明细
- 定制宠物
- 反馈、关于、引用经典

### 兼容入口

- 旧 assessments 路径
- 旧 register 路径
- 旧分享路径
- 带 inviteCode/caseId/targetEventId 的直达路径
- `monthly-review` 分享落地路径及其 `caseId`
- `quick-read` 分享落地路径及其完整分享参数
- `pair-onboarding` 分享回流和配对发起参数
- `taohua-pair-result` 配对结果直达参数
- `taohua-pair-share` 配对分享回流参数
- `taohua-persona-result` 桃花人设分享回流参数

## 5.3 视觉与资源检查

- 所有 SVG 图标正常显示。
- Tab 图标/自定义 Tab 样式正常。
- 两只宠物头像、动画和自定义宠物正常。
- 分享按钮和分享图正常。
- 今日的 TA、本次分析、行动指南渐变样式正常。
- 字体大号模式不溢出。
- 安全区和底部 Sheet 不被 Tab 遮挡。

## 6. 上线前检查单

- [ ] 使用生产构建目录 `dist/build/mp-weixin`
- [ ] 6 个受保护分享落地页均已恢复，且 `pages.json` 注册页全部存在
- [ ] 使用 `npm.cmd run build:mp-weixin` 完成正式构建和后处理
- [ ] 微信开发者工具清除全部缓存后重新编译
- [ ] 主包实际上传体积 < 2048KB，不再出现 `80051`
- [ ] 记录实际上传体积及剩余余量，确认至少保留约 30KB 临时余量
- [ ] `pages.json` 未新增 `subPackages`，没有页面目录或页面 URL 变化
- [ ] 无失效页面路径
- [ ] 无丢失 SVG/PNG
- [ ] 邀请链接、分享链接正常
- [ ] 五个 Tab 正常
- [ ] 未登录/已登录/onboarding 三种状态正常
- [ ] 专项测试和全量回归通过
- [ ] H5、微信小程序构建通过
- [ ] 微信真机预览通过
- [ ] 完成一次真实体验版上传
- [ ] 记录整改前后体积对比

## 7. 推荐执行顺序

1. 先完成阶段 0，恢复并验证 6 个受保护分享落地页，固定可构建基线。
2. 修复并验证正式构建后处理脚本的读取/执行权限，跑通 `npm.cmd run build:mp-weixin`。
3. 完成阶段 A 的两个重复 Logo 和确认无运行时引用静态副本的忽略规则。
4. 执行完整回归、H5/微信构建、真机预览和体验版上传，确认 `80051` 已消失。
5. 阶段 C 只完成 `register/assessments` 入口调查，不修改页面。
6. 上传问题解决后，再单独清理四个已复核无引用的源码组件；一次一组、单独提交、逐次回归。
7. 本轮到此结束，不进入分包、页面迁移、样式重构、规则模块拆分或图片格式替换。

该顺序先完成可回滚、可验证的低风险事项。分包和其他深度优化保留在暂缓区，后续只有再次明确批准后才重新制定实施方案。

## 8. 2026-07-28 执行记录

已完成：

- 恢复 6 个受保护分享落地页；微信端 28 个注册页面均生成完整产物。
- 恢复 H5 后台仍在直接引用的 8 个面板组件及公共样式。
- 修复 `scripts/postbuild-mp-weixin.cjs` 访问权限，正式后处理可正常执行。
- `packOptions.ignore` 共生成 13 条规则：2 个 Logo/App 图标、1 个旧 TabBar 目录、10 个无运行时引用 SVG 副本。
- 正式微信产物共 316 个文件，后处理后原始体积约 1306.5KB；扣除产物中实际存在的 ignore 目标后约 1238.8KB。
- 构建产物未出现 `subPackages`，页面路径未改变。
- 删除 4 个无源码引用且未进入构建产物的旧组件：`AssessmentTrendChart.vue`、`ProfileInline.vue`、`CaseProfileFields.vue`、`ProfileAvatar.vue`。
- `test:regression`、`test:pet-energy` 通过。
- H5、微信小程序、App 三端生产构建通过。

仍需人工完成：

- 微信开发者工具导入 `dist/build/mp-weixin`，清除缓存后重新编译。
- 真机检查图标、分享入口和 6 个受保护分享落地页。
- 人工确认版本号和备注后上传体验版，记录微信处理后的实际体积；不提交审核、不发布正式版。
