# 微信小程序主包整改计划

日期：2026-07-28

项目：`workspace-girl-uniapp`

目标：解决微信上传错误 `80051`，清理确认无效的资源和代码，并通过分包为后续迭代留出稳定空间。

## 1. 当前基线

### 1.1 当前体积

| 项目 | 当前值 |
| --- | ---: |
| `dist/build/mp-weixin` 原始文件总量 | 约 1360.6KB |
| 排除重复 `logo.png`、`app-icon.png` 后 | 约 1237.3KB |
| 微信开发者工具报出的上传源代码体积 | 2081KB |
| 微信主包限制 | 2048KB |
| 当前超限 | 约 33KB |

说明：磁盘目录体积与开发者工具上传体积不同，是因为开发者工具还会进行 ES6 转译、代码处理和打包计算。整改不能只看文件夹体积，最终以微信开发者工具“上传”前的代码体积为准。

### 1.2 已完成的止血措施

以下两个文件内容完全相同，且小程序编译产物没有引用：

- `static/logo.png`：约 61.7KB
- `static/app-icon.png`：约 61.7KB

已在 `src/manifest.json` 与根目录 `project.config.json` 中加入微信打包忽略规则，正式构建产物已经正确生成对应 `packOptions.ignore`。

预计上传体积从 2081KB 降至约 1958KB。该结果可以解决本次上传问题，但安全余量仍只有约 90KB，不足以支撑持续迭代，因此必须继续进行结构整改。

## 2. 审计结论

### 2.1 主要体积来源

| 内容 | 体积 | 判断 |
| --- | ---: | --- |
| 全部页面代码 | 约 693.7KB | 最大结构问题，28 个页面全部位于主包 |
| JavaScript | 约 499.8KB | 大部分有效，主要来自页面逻辑和公共规则模块 |
| WXSS | 约 330KB | 首页、桃花页、关系详情占比较高 |
| PNG | 约 218.6KB | 两只宠物图片约 92.4KB；两个重复 Logo 已排除 |
| SVG | 约 103.7KB | 包含动态表情图标和部分重复静态副本 |
| WXML | 约 199.4KB | 桃花页、关系详情和首页较大 |

最大的页面：

- 首页：约 114KB
- 桃花页：约 108.5KB
- 关系详情：约 78.3KB
- 时间线：约 49.3KB
- 我的：约 39.6KB

最大的公共模块：

- `common/vendor.js`：约 65.7KB
- `utils/insights.js`：约 44.1KB
- `utils/taohua.js`：约 29.7KB
- `utils/theme.js`：约 14.4KB
- `utils/api.js`：约 12.2KB

完整引用审计没有发现可以直接删除的公共工具模块或未被调用的导出函数。`insights.js` 和 `taohua.js` 虽然较大，但仍被多个核心页面使用，不列入直接删除范围。

### 2.2 确认可从上传包排除的静态副本

除两个重复 Logo 外，正式产物中还有约 6.1KB 文件没有任何运行时引用：

- 整个旧 `static/tabbar` 图片目录，共 10 张图片
- `needle-flower.svg`
- `stars.svg`
- `needle-heart.svg`
- `heart.svg`
- `list-checks.svg`
- `book-open.svg`
- `share-2.svg` 的 `static` 副本
- `alert-triangle.svg`
- `needle-star.svg`
- `check-circle.svg`

注意：部分图标功能仍在使用，但 Vue 编译后引用的是 `/assets/文件名.hash.svg`。这里要排除的是 `static` 目录里的重复副本，不能删除编译生成的 `/assets` 文件。

### 2.3 确认无源码引用的旧组件

完整源码扫描没有发现以下组件被任何页面或组件导入：

| 文件 | 源码体积 | 对上传包的影响 |
| --- | ---: | --- |
| `AssessmentTrendChart.vue` | 约 8.8KB | 未进入构建产物，无减包收益 |
| `ProfileInline.vue` | 约 5.6KB | 未进入构建产物，无减包收益 |
| `CaseProfileFields.vue` | 约 5.2KB | 未进入构建产物，无减包收益 |
| `ProfileAvatar.vue` | 约 1.6KB | 未进入构建产物，无减包收益 |

这些文件属于仓库卫生问题，不是本次上传失败的原因。为减少风险，应放在最后阶段清理。

### 2.4 疑似遗留页面

以下页面只在 `pages.json` 注册，完整编译产物中没有任何内部页面跳转到它们：

| 页面 | 体积 | 风险判断 |
| --- | ---: | --- |
| `pages/assessments/assessments` | 约 27.9KB | 很可能已被关系详情、时间线取代 |
| `pages/register/register` | 约 5.2KB | 当前登录页没有注册入口，但可能存在邀请链接或历史直达链接 |

不能仅凭“内部引用为 0”直接删除。删除前必须确认：

1. 是否存在历史二维码、小程序消息、公众号菜单或客服话术直达该路径。
2. 是否存在云函数、数据库配置或运营后台动态下发该路径。
3. 是否需要兼容旧分享卡片。
4. 邮箱注册是否仍作为微信登录失败时的兜底方式。

## 3. 整改目标与验收线

### 3.1 必须达到

- 微信开发者工具可以正常预览、真机调试和上传。
- 不再出现错误码 `80051`。
- 主包经开发者工具处理后的上传体积不高于 1700KB。
- 至少保留 300KB 的主包安全余量。
- `dist/build/mp-weixin` 中主包原始文件建议控制在 950KB 以内。
- 每个分包必须低于 2MB；全部包总量必须低于微信平台总包限制。
- 现有分享路径、邀请参数、Tab 页面和登录重定向全部可用。

### 3.2 不作为本轮首要目标

- 不为追求几 KB 收益大规模重写业务逻辑。
- 不直接压缩或替换宠物图片，除非分包后仍无法达到目标。
- 不重构 `insights.js`、`taohua.js` 的规则算法，避免引入分析结果回归。
- 不删除外部入口尚未确认的页面。

## 4. 分阶段执行方案

## 阶段 A：安全止血与包规则固化

目标：确保当前版本可以上传，并保证正式构建每次都自动应用忽略规则。

### A1. 固化现有重复 Logo 忽略规则

- 保留 `src/static/logo.png` 和 `src/static/app-icon.png`，避免影响 App/H5 构建。
- 仅在微信小程序 `packOptions.ignore` 中排除。
- 同时维护：
  - `src/manifest.json`
  - 根目录 `project.config.json`
- 每次生产构建后检查 `dist/build/mp-weixin/project.config.json`，确认规则没有被覆盖。

### A2. 排除确认无引用的 6.1KB 静态副本

- 优先使用 `packOptions.ignore`，暂不删除源文件。
- 构建后逐项确认：
  - `/assets/*.hash.svg` 仍存在。
  - 运行时不存在指向被忽略 `/static/...` 文件的路径。
  - 首页、三个分析弹窗、行动指南、桃花页、十二生肖/星座图标正常显示。

### A3. 上传验证

- 微信开发者工具必须打开 `dist/build/mp-weixin`，不能使用旧的 `dist/dev/mp-weixin`。
- 清除全部缓存并重新编译。
- 在“代码依赖分析/代码质量”中记录主包体积。
- 执行一次真实上传，记录上传后的最终体积和版本号。

### 阶段 A 回滚

- 如果任何图标缺失，只回退对应的 ignore 条目。
- 不回退已经验证安全的两个重复 Logo 规则。
- 不删除任何源资源，因此回滚只涉及配置文件。

## 阶段 B：主包分包改造

目标：从结构上释放约 250–380KB 主包空间，解决后续持续增长问题。

### B1. 主包保留范围

以下页面必须保留主包：

- `pages/login/login`：启动页
- `pages/index/index`：Tab 页
- `pages/cases/cases`：Tab 页
- `pages/case-detail/case-detail`：Tab 页
- `pages/timeline/timeline`：Tab 页
- `pages/me/me`：Tab 页

微信 `switchTab` 目标不能放入分包；首个启动页面也必须位于主包。

当前这 6 个页面合计约 307.4KB。

### B2. 建议分包分组

#### 桃花功能包，约 178.6KB

- `taohua`
- `taohua-share`
- `taohua-pair-share`
- `pair-onboarding`
- `taohua-pair-result`
- `taohua-persona-result`

#### 分析工具包，约 81.9KB

- `assessments`
- `quick-read`
- `monthly-review`
- `reassess`
- `new`

#### 画像与支持包，约 78.1KB

- `self-profile`
- `edit-profile`
- `explain`
- `references`
- `custom-pet`
- `feedback`
- `about`

#### 账户与付费包，约 42.4KB

- `subscription`
- `token-usage`
- `token-recharge`

#### 注册兼容页，约 5.2KB

- `register`

### B3. 路径兼容策略

分包过程中最容易出岔子的是路径变化。执行前必须建立“旧路径 → 新路径”映射表，并扫描：

- `navigateTo`
- `redirectTo`
- `reLaunch`
- `switchTab`
- `onShareAppMessage`
- `onShareTimeline`
- 邀请码 `redirect` 参数
- 云函数返回的页面路径
- 公众号/二维码/运营配置中的外部路径

优先方案：尽量通过分包根目录配置保留原有 URL。若必须移动目录，则对可能存在历史外链的页面保留轻量兼容页，负责校验参数并跳转到新页面。

禁止直接删除旧路径后依赖“用户不会再点旧链接”。

### B4. 分包顺序

为降低一次性故障面，按以下顺序逐包改造，每完成一包立即编译和冒烟：

1. 账户与付费包：业务边界清晰、页面相对独立。
2. 画像与支持包：重点验证 onboarding 和编辑画像跳转。
3. 分析工具包：重点验证 caseId、targetEventId、redirect 参数。
4. 桃花功能包：链路最长，放在最后处理。

不要一次性移动全部 22 个页面后再统一排错。

### B5. 公共依赖处理

- 非独立分包优先，避免每个分包重复携带 Vue/runtime。
- `vendor.js`、`api.js`、`theme.js`、`helpers.js` 等公共模块保持主包共享。
- 检查分包页面是否错误复制公共工具，导致总包增长。
- 仅分包专用的大组件或素材才跟随进入分包。
- 宠物资源仍被首页使用，不能整体移动到分包。

### 阶段 B 回滚

- 每个分包单独形成一组改动，不与业务功能修改混合。
- 若某一分包失败，只回退该分包的 `pages.json`、目录和路由改动。
- 主包保留页面不得在整改过程中改业务逻辑。

## 阶段 C：遗留页面处理

### C1. `assessments` 页面

建议优先确认其产品定位。

- 如果已完全由时间线取代：保留一个轻量兼容页，将 `caseId` 写入活动档案并跳转到时间线。
- 如果仍需要独立分析历史：移入分析工具分包，不删除。
- 只有确认没有历史外链后才彻底删除。

### C2. `register` 页面

- 确认当前生产环境是否仍支持邮箱密码登录。
- 确认邀请链接是否会直达注册页。
- 若微信小程序只使用微信自动登录：可以改为轻量兼容页，将邀请码保存后跳转到登录/欢迎页。
- H5 如仍需要邮箱注册，应通过条件编译保留 H5 页面，不能全平台直接删除。

### C3. 验收

- 直接访问两条旧页面路径。
- 带 `caseId`、`inviteCode`、`redirect` 参数访问。
- 未登录、已登录、资料未完成三种状态分别验证。
- 验证不会形成重定向循环。

## 阶段 D：仓库死代码清理

目标：改善维护性，不作为上传问题的前置条件。

### D1. 删除前再次确认

对四个疑似死组件执行：

- 完整源码搜索。
- H5、小程序、App 三端生产构建。
- 检查是否被动态组件名、easycom 或配置文件引用。

### D2. 清理方式

- 一次只删除一组确定无引用组件。
- 删除后执行全量回归和三端构建。
- 不顺手清理其他看起来“旧”的文件，避免扩大变更范围。

## 阶段 E：可选深度优化

只有在完成分包后，主包仍高于目标线时才执行。

### E1. 页面样式体积

重点审计：

- 首页 WXSS：约 38.6KB
- 桃花页 WXSS：约 38.1KB
- 关系详情 WXSS：约 33.2KB

检查公共 Campus Pop 规则是否在多个页面重复展开。可将真正的全局基础样式放到 `App.vue/app.wxss`，组件级样式继续保持 scoped，避免产生样式串页。

### E2. 规则模块拆分

- `insights.js` 和 `taohua.js` 只在确认分包编译仍将全部代码放进主包时再考虑拆分。
- 按调用页面拆成纯函数模块，不改变任何算法阈值或文案。
- 必须增加规则边界测试后才能实施。

### E3. 图片优化

- 检查两只宠物 PNG 是否可以无损压缩。
- 不直接改 WebP，先确认微信基础库、App、H5 和自定义宠物加载链路均兼容。
- 图片优化后必须逐帧检查透明背景和尺寸，避免宠物精灵错位。

## 5. 完整验证矩阵

## 5.1 自动化检查

每个阶段必须执行：

```text
npm.cmd run test:pet-energy
npm.cmd run test:regression
npm.cmd run build:h5
npx.cmd uni build -p mp-weixin
```

条件允许时补充：

```text
npm.cmd run build:app
```

同时执行：

- `git diff --check`
- 校验 `app.json` 的 `pages`、`subPackages`、Tab 页面。
- 校验 `project.config.json` 的 `packOptions.ignore`。
- 统计主包、每个分包和总包体积。
- 搜索构建产物中的失效路由和缺失静态文件引用。

## 5.2 微信端冒烟路径

### 启动与登录

- 冷启动
- 已登录启动
- 未登录启动
- 邀请码启动
- 资料未完成时进入本人画像
- 带 redirect 参数启动

### 五个 Tab

- 今日
- 我们
- Crushes
- 往事
- 我

### 核心关系流程

- 新建 Crush
- 快速记录
- AI 分析完成
- 本次分析弹窗
- 行动指南弹窗
- 今日的 TA 弹窗
- 时间线
- 重新分析
- 月度复盘

### 桃花流程

- 桃花首页
- 桃花人格分享
- 配对 onboarding
- 配对结果
- 配对分享
- 人设结果
- 分享卡片回流

### 账户和个人资料

- 本人画像
- 编辑 Crush 画像
- 订阅
- Credits 充值
- Credits 明细
- 定制宠物
- 反馈、关于、引用经典

### 兼容入口

- 旧 assessments 路径
- 旧 register 路径
- 旧分享路径
- 带 inviteCode/caseId/targetEventId 的直达路径

## 5.3 视觉与资源检查

- 所有 SVG 图标正常显示。
- Tab 图标/自定义 Tab 样式正常。
- 两只宠物头像、动画和自定义宠物正常。
- 分享按钮和分享图正常。
- 今日的 TA、本次分析、行动指南渐变样式正常。
- 字体大号模式不溢出。
- 安全区和底部 Sheet 不被 Tab 遮挡。

## 6. 上线前检查单

- [ ] 使用生产构建目录 `dist/build/mp-weixin`
- [ ] 微信开发者工具清除全部缓存后重新编译
- [ ] 主包上传体积 ≤ 1700KB
- [ ] 主包至少保留 300KB 余量
- [ ] 每个分包 < 2MB
- [ ] 总包未超平台限制
- [ ] 无失效页面路径
- [ ] 无丢失 SVG/PNG
- [ ] 邀请链接、分享链接正常
- [ ] 五个 Tab 正常
- [ ] 未登录/已登录/onboarding 三种状态正常
- [ ] 专项测试和全量回归通过
- [ ] H5、微信小程序构建通过
- [ ] 微信真机预览通过
- [ ] 完成一次真实体验版上传
- [ ] 记录整改前后体积对比

## 7. 推荐执行顺序

1. 先完成阶段 A，并重新上传当前版本，确认 `80051` 已消失。
2. 以“账户与付费包”为试点完成第一个分包，验证技术路径。
3. 按画像支持、分析工具、桃花功能顺序逐包迁移。
4. 确认旧 register/assessments 的业务处置方案。
5. 最后清理四个死源码组件和静态源文件。
6. 只有主包仍高于 1700KB 时，才进入样式、规则模块和图片深度优化。

该顺序优先解决上传问题，同时把高风险的路由调整和业务删除放到充分验证之后，避免一次性大改导致无法定位问题。
