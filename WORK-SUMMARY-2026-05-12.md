# 今日工作总结 - 2026-05-12

## 一、整体产品逻辑梳理

今天重新梳理了小程序的信息架构：

- 首页定位为“即时记录、即时分析”。
- 关系页定位为“统计、复盘、总结”。
- 时间轴定位为“历史记录”，同时承载关键事件流和评估历史流。
- 即时反馈、评估历史、侧写、时间轴之间的关系进一步统一，避免用户看完当前分析后无法追溯历史来源。

## 二、即时反馈与评估历史统一

围绕“即时反馈快照”做了多轮调整：

- 评估历史中的即时反馈快照尽量对齐首页即时反馈内容。
- 删除评估历史中重复的意向变化、风险变化文字。
- 调整横条图位置，让意向变化和风险变化更直观。
- 当前状态改为更简洁的标签和一句话状态，避免空话和重复分数。
- 侧写历史从评估历史中移除，概念上让评估历史更明确地挂钩“即时反馈”。

## 三、首页功能恢复与体验调整

今天持续修正首页被弱化的问题：

- 保留一句话快速记录后的即时反馈展示。
- 新增或恢复上传附件、图片预览、上传后链接展示等体验要求。
- 角色判断放到一句话快速记录下方，支持用户手工选择，也支持后续 AI 自动判断。
- 下一次进入首页时显示上一次即时反馈，本次录入完成后再更新为本次即时反馈。
- 删除最近 7 天回顾、观察成就、最近更新列表，让首页更聚焦即时记录和即时分析。
- 侧写移动到首页即时反馈下方，作为参考内容；关系页不再体现侧写。

## 四、AI 即时反馈提示词迭代

今天重点改进了 `createTimeline` 云函数里的 AI 分析提示词。

核心方向：

- AI 角色定位调整为“情场高手 / 闺蜜型关系顾问”。
- 用户可见内容里避免出现“用户”这个词，统一用“你”。
- 结果要直接、具体、简练，不解释 AI 方法论。
- “判断依据”“你接下来怎么做”“侧写”必须承接同一次事件语义理解。
- 如果判断依据识别出开房、性兴趣、亲密边界等信息，后续建议必须跟着这个语义走，不能回到普通邀约模板。
- “你接下来怎么做”改为 AI 语义生成，不再依赖硬规则模板。

## 五、亲密场景语义校准

针对测试中“约我去小树林”被后续建议过度套成“开房”模板的问题，今天做了提示词修正：

- AI 可以内部做语义强度校准。
- 输出必须贴着用户原文和场景。
- “小树林”应围绕私密、暧昧、单独相处、边界试探来分析。
- “酒店 / 开房 / 过夜”才围绕性边界、身体兴趣、关系节奏来分析。
- 不允许所有私密邀约都套成同一个开房回应。

## 六、属相与星座侧写调整

今天明确了属相和星座侧写的方向：

- 属相按中国传统生肖 / 命理语境解释。
- 星座按西方占星语境解释。
- 侧写必须和本次事件强关联，不能写泛泛百科。
- 如果对方属鸡并提出私密邀约，AI 应结合生肖鸡的命理说法和本次行为分析其心理与应对。
- 如果涉及星座，也要按对应星座的占星特质解释本次行为，不做空泛性格描述。

## 七、云函数与构建验证

今天多次修改并部署 `createTimeline` 云函数，重点让 AI 提示词在线上小程序生效。

已完成验证：

- `npm.cmd run build:mp-weixin` 多次构建通过。
- `createTimeline` 云函数已部署成功。
- 最近一次确认的云端修改时间为 `2026-05-12 20:35:04`。

## 八、仍需继续观察的问题

后续测试重点：

- 新增事件后，AI 是否真的根据原文语义生成非模板化回答。
- “小树林”“酒店”“开房”“牵手”“一起吃饭”等不同事件是否能给出明显不同的建议。
- 属相和星座侧写是否既有命理 / 占星味道，又能贴合本次事件。
- 评估历史里的即时反馈快照是否与首页即时反馈保持一致。
- 云函数调用是否仍可能接近 30 秒超时，需要继续观察日志。

